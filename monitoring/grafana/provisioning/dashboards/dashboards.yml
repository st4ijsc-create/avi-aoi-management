# Phase 1 WS1.5 — Grafana dashboard provider.
apiVersion: 1
providers:
  - name: synapse
    orgId: 1
    folder: SYNAPSE
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
