# Workflows

Các quy trình end-to-end chạy qua **nhiều chức năng** (vd: "Tạo lô mới → kiểm tra → xuất báo cáo → đóng lô").

Mỗi file mô tả 1 workflow, mỗi bước link tới file feature tương ứng trong `../features/`.
