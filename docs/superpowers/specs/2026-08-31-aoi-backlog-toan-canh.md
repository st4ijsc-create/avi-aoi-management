# AOI/AVI — Backlog toàn cảnh sau năm pha vá

**Ngày:** 2026-08-31 · **Nhánh:** `feat/hmi-dep` · **Spec gốc:** `2026-08-24-aoi-5-cap-xuong-song-design.md`

Tài liệu này gom **toàn bộ** món nợ đang mở vào một chỗ đọc được. Trước nó, 77 mục BG nằm rải trong spec §13, năm sổ SDD (**thư mục git-ignored**), và hàng chục báo cáo task.

⚠ **Bài học đã trả giá:** ngày 2026-08-30 phát hiện **14 mục bàn giao chỉ tồn tại trong sổ git-ignored** — một lượt `git clean -fdx` là mất sạch, gồm hai mục ⛔. Sổ SDD là **bản nháp phục hồi**; spec và tài liệu này mới là **bản bàn giao**.

---

## 1. Đã làm gì — năm pha, sáu lượt review

| Pha | Kết quả | Review toàn nhánh tìm ra |
|---|---|---|
| **1A** nền cây 4 cấp | 5 task · 68 ca · migration 0338/0339 | **2 Critical** |
| **1B** ingest cây | 8 task · 151 ca · migration 0340 | **4 Critical** — một cái **bác bỏ lời khai trung tâm** của người điều phối |
| **1C** vá 4 lỗ dữ liệu | 5 task · 166 ca | — |
| **WAL** cho cây v2.0 | 3 task · 72 ca | census **tự tìm** 1 Critical ở cửa BOOT |
| **1C+WAL** (gộp) | — | **2 Critical** — **cửa ingest thứ SÁU** đang chạy thật, hai census đều mù |
| **1D** trước Khối B | 6 task · 395 ca · migration 0344 | **3 Critical** — **hai là bản vá của chính pha đó tự gây hại** |
| **Đ-24** | 1 task | — |
| **lượt 5** (Đ-24 + BG-52) | — | **5 Critical** — **bác bỏ mốc son "ba đường ingest cùng luật"** |
| **1E** sáu mục ⛔ | 3 task · 426 ca | *(lượt 6 đang chạy)* |

**Tổng: 31 task, ~1.200 ca lưới, 6 migration. Review toàn nhánh tìm Critical 5/5 lượt đã xong.**

### Điều đáng nói nhất
**21 review-theo-task bỏ qua những thứ review toàn nhánh bắt được** — vì chúng nằm ở **chỗ các task ghép vào nhau**, không nằm trong task nào.

Và **ba lần liên tiếp một bản vá ĐÚNG đã mở một chiều khác**:
- Trần `attempts` (chống chặn-đầu-hàng) ⇒ **vứt bo** khi DB gián đoạn từng phần.
- Gỡ chặn-đầu-hàng ⇒ **gỡ luôn trần chi phí** ⇒ một tick = 40.000 lời gọi DB.
- Thêm `.max()` (đúng) ⇒ **chuyển lỗi** từ `Postgres 22001` (vĩnh viễn) sang `ZodError` (tạm thời) ⇒ chốt chặn retry **không còn bắn** cho đúng lớp payload nó nhắm tới.

⇒ Từ Pha 1E, mọi brief mang một câu hỏi bắt buộc: ***"bản vá này chuyển thứ gì từ lớp nào sang lớp nào, và ai đang phụ thuộc vào phân lớp cũ?"*** Hai trong ba task của Pha 1E **tự bắt được một lỗ** nhờ nó — lần đầu lớp lỗi đó chết tại bàn thay vì phải chờ review.

---

## 2. Bốn lớp lỗi lặp lại — nhận diện để không tái phạm

### L-1. Lưới cưỡng chế **DANH SÁCH** thay vì cưỡng chế **BẤT BIẾN** (4 lần)
| Lần | Lưới | Cơ chế hỏng |
|---|---|---|
| 1 | BG-14 canh `serialNumber` ở ingest | **regex trên văn bản** ⇒ soi đúng dòng của nhánh **không liên quan** ⇒ xanh giả |
| 2 | Census cửa ingest | tìm cửa bằng **quy ước đặt tên** ⇒ bỏ lọt **cửa thứ sáu** đang chạy với 10 bo |
| 3 | Census `.max()` | ghim `bảng.length === 30`, lặp trên **chính bảng đó** ⇒ xanh giả khi ai đó **thêm** trường |
| 4 | Walker zod | `return []` **im lặng** với 6 loại nút chưa hỗ trợ |

**Điểm chung:** *im lặng ở chỗ không biết* đọc **giống hệt** *xanh vì không có vấn đề*.
**Cách chống đã dùng:** quét **AST** không quét văn bản · duyệt **schema** không duyệt bảng · **báo động** khi gặp nhánh lạ · và ca **chống-tự-thoả** (lưới phải khẳng định nó tìm thấy ≥N đối tượng).

### L-2. Cơ chế **tồn tại nhưng chưa nối** (5 lần)
`loiMayChuaNangCap` (0 điểm gọi) · `verdictLuuTru` (chưa nối tới Pha 1B T5) · luật `integrityScan` (tính nhưng giao diện không vẽ) · `ntfSource` (cột có, không ai ghi) · `submitInspectionCoreObject` **được export "để census soi"** mà 0 file import.
**Chống:** đòi bằng chứng **hành vi đầu-cuối**, không nhận "hàm tồn tại". Chú thích phải nói rõ **chưa nối**.

### L-3. Chú thích **khai quá** so với hành vi (5 lần)
Ví dụ nặng nhất: bảng miễn trừ census WAL ký lý do *"mutation do NGƯỜI kích hoạt"* cho cửa ZIP — trong khi docblock của **chính file đó** ghi *"Agent gọi"*. **Một lý do sai trong bảng dựng ra để trung thực nguy hiểm hơn không có lý do**: nó khiến mọi review sau tin câu hỏi đã được cân nhắc đúng. Đó là cách cửa thứ sáu sống sót qua hai lượt census.

### L-4. **Phạm vi đo sai** — đo một tập rồi phát biểu cho tập khác (8 lần)
Đo `dev` rồi khai cho mọi DB · đo 238 gói (đều đã là NG) rồi suýt khai "chống hồi quy mạnh" · khai "không có nguy cơ cắt cụt" từ **một** tệp mẫu.
**Chống:** mọi lời khai kèm **tập đã đo**; và hỏi *"tập này có chứa hình dạng cần đo không?"*

---

## 3. Backlog đang mở — phân theo mức

### ⛔ CHẶN — phải xong trước hoặc cùng Khối B

| Mã | Việc | Số đo |
|---|---|---|
| ~~**BG-39 gđ2**~~ | **ĐÓNG-cờ-TẮT (2026-09-05, Lô 3 Mục 3, `c69e071c`) — chờ số BG-57a để bật.** `commit` (`aoiPackageRouter.ts`) nay tự gác hình dạng phẳng ở `meta.json`: đọc JSON THÔ (`metaRaw`) rồi hỏi `ingestRejectLegacyMachineEnabled() && !laHinhDangCayV2(metaRaw)` **TRƯỚC** `metaJsonSchema.parse()` — CÙNG bộ nhận diện/thông điệp với đường v1 trực tiếp (`laHinhDangCayV2`, `loiMayChuaNangCap`, `ingestRejectLegacyMachineEnabled`, export dùng chung, không viết bản thứ hai). ⚠ Đo trước khi vá: `metaJsonSchema` (BG-85) yêu cầu `surfaces` **bắt buộc** ⇒ một payload PHẲNG THẬT không bao giờ tới được `metaData` (ném `ZodError` ngay tại `.parse()`), và `laHinhDangCayV2(metaData)` **sau** parse luôn `true` (kể cả `surfaces=[]`) — gác phải hỏi trên JSON thô, hỏi sau parse là mã chết. Cờ TẮT (mặc định) ⇒ hành vi hôm nay **không đổi 1 byte** (đã xác nhận lại: mệnh đề 4 `aoiPackageBienBg85.test.ts` vẫn xanh nguyên văn). Quyết định "gói đã trong kho khi cờ TẮT rồi commit lúc cờ BẬT": **từ chối như lượt live** — bo không mất (còn nguyên trong storage), không phá mệnh đề WAL/dead-letter (`loiMayChuaNangCap` là `Error` thường ⇒ xếp TẠM THỜI, không đếm vào ngưỡng `'dead'`) | `aoiPackageZipGacMayCu.test.ts` (6 ca, RED xác nhận bằng revert HEAD: 4/6 đỏ đúng — 2 ca cờ-BẬT + 2 ca đột biến-neo-mã-nguồn; 2 ca còn lại xanh cả hai phía đúng ý — cờ-TẮT không đổi, payload cây hợp lệ không bị cắt oan). tsc sạch, 44/44 test liên quan (BG-85 + census + BG-89) xanh |
| ~~**Đ-19**~~ | **ĐÓNG** (Khối B Task 3 `b7bdd013` + Khối C). Cấp component ghi được: `measurement_results` có `inspectionCaptureRowId` + `componentExtId` + `pointDefId` tra đúng máy + đúng sản phẩm. **Phần giới hạn** đóng ở Khối C: cây dạy không mang giới hạn (đo mẫu thật) ⇒ kỹ sư dạy trên hệ qua `setLimitsBatch` (`fc232773`) + dialog (`0d570915`/`8d3e26c5`); cổng ra mệnh đề 3 (`343f8af9`) chứng minh chuỗi cây dạy → giới hạn → kết quả → cổng chấm: `specGate.dat+truot` **0→3** trên v2 | Trước: `measurement_results` nối cây = 0. Sau: 16 hàng/bo (mẫu 2/4/8/16), `aoi_management_test`. ⚠ Còn mở quanh nó: BG-111 (không cầu nối duyệt), BG-112 (fixture lệch 32/48), BG-113 (server không kiểm ≤), cờ snapshot v2 mặc định TẮT |

### ★★ NÊN LÀM SỚM — sai ngữ nghĩa, hộ tiêu thụ tương lai sẽ sai âm thầm

| Mã | Việc | Số đo |
|---|---|---|
| ~~**BG-36**~~ | **ĐÓNG-đọc (2026-09-05, Lô 4 Mục 3) — dead-letter WAL + integrityScan nay có ĐƯỜNG ĐỌC (phạm vi ĐỌC-only, chưa resubmit/replay).** `server/services/inspection/deadLetterReader.ts` (module mới) đọc file dead-letter thật qua `listDeadLetterEntries` (phân trang + tổng số + tổng byte thật từ `fs.stat`, không bao giờ trả payload đầy đủ) + `getDeadLetterDetail` (payload cắt gọn AN TOÀN: chuỗi >2000 ký tự bị cắt kèm độ dài gốc, `apiKey` bị xoá, trần tổng 60KB). `aoiPackageRouter` thêm `listDeadLetters`/`getDeadLetterDetail` (`protectedProcedure`+`requirePermission("admin_system","canView")`, cùng nhóm quyền `systemHealthRouter.dbIngestHealth`). `integrityScan` HOÁ RA đã có `integrityRouter.ts` (summary/runNow/history) VÀ đã persist thật vào `integrity_scan_results` từ trước (216 hàng, 21 scanKey, weekly cron) — lỗ thật chỉ là **0 UI nào gọi nó**; đóng bằng tab mới trong `AOIPackages.tsx` đọc `integrity.summary` có sẵn, lọc xuống 6 khoá liên quan ingest qua `ingestIntegrityScanPresentation.ts` | Đo trước: `data/inspection-store-forward.dead.jsonl` = **101 dòng, 7.410.317 byte** (khớp con số khai gốc). Đo sau (script gọi thẳng hàm, không qua HTTP): `listDeadLetterEntries` trả `total=101, totalBytes=7410317`; `getDeadLetterDetail` cho mục đầu = 3417 byte sau cắt gọn (so với dòng gốc lớn hơn nhiều lần). `integrity_scan_results`: `current_database()=aoi_management`, 216 hàng, 21 scanKey — persist đã có sẵn từ trước Lô 4 |
| **BG-74** | ~~`'dead'` gần vô hình: `listPackages` không lọc được · `getStats` cộng không khớp · badge **xám nhạt** hơn `failed` (đỏ) · thiếu i18n · tài liệu Agent vẫn 5 giá trị~~ **ĐÓNG (2026-09-05, Lô 4 Mục 1+2).** Server: `listPackages.input.status` enum mở rộng đủ 6 giá trị (`dead` thêm); `getUploadStats` (tên thật của "getStats" — không có procedure tên `getStats` trong `aoiPackageRouter.ts`) thêm nhóm `dead` riêng, bất biến CỘNG-TỔNG `committed+failed+pending+dead=total` được ghim bằng test trên DB thật (gói `'dead'` dựng qua ĐƯỜNG THẬT — 3 lượt `commit()` lỗi vĩnh viễn liên tiếp, không set thẳng cột). `AoiPackageSection.tsx` (apiDocs) cập nhật đủ 6 giá trị. Client: `AOIPackages.tsx` — badge `'dead'` dùng nền ĐẶC `bg-destructive` (không mờ `/15` như `failed`) + chữ trắng, nổi hơn rõ ràng; bộ lọc `<Select>` thêm `'dead'`; i18n additive 3 locale (`packages.dead`) không dùng chung `common.error` với `failed`. Logic badge/filter rút thành module thuần `aoiPackagesStatusPresentation.ts` (repo không có hạ tầng render-test — BG-129 — nên test được không cần render component) | Server: 3 ca RED→GREEN (`aoiPackageDeadCongDanHangNhat.test.ts`), 119/119 test `aoiPackage*` liên quan vẫn xanh, tsc sạch. Client: 6 ca (`aoiPackagesStatusPresentation.unit.test.ts`), viStringCoverage 8/8, i18n:check sạch (0 mismatch/missing MỚI) |
| **BG-70** | `demSoLoiVinhVienTuLichSu` đếm **cộng dồn**, chú thích khai **"LIÊN TIẾP"** ở 5 chỗ | gói lỗi 4 lần rồi sửa vẫn **cách `'dead'` đúng 1 lỗi** |
| **BG-71** | Phân loại vĩnh viễn **đảo ngược** ở cửa ZIP: "ZIP not found" (ổ mạng rớt) tính vĩnh viễn; `TOO_MANY_REQUESTS` trong `PERMANENT_TRPC_CODES` | |
| **BG-72** | `.max(40)` từ chối `DateTime.ToString()` mà `new Date()` **vẫn nhận** | 50 và 57 ký tự bị từ chối |
| **BG-73** | ✅ **ĐÃ QUYẾT (chủ dự án, 2026-09-01): BỎ hướng sửa schema.** Hướng (b) ở `8150ab6d` là lời giải CUỐI — lệch hình dạng **không đếm** vào ngưỡng `'dead'`, gói nằm `'failed'` và retry vô hạn | mẫu chuẩn `safeParse` = **false**; hệ quả đã duyệt |
| **BG-74** | `'dead'` gần vô hình: `listPackages` không lọc được · `getStats` cộng không khớp · badge **xám nhạt** hơn `failed` (đỏ) · thiếu i18n · tài liệu Agent vẫn 5 giá trị | |
| **BG-75** | `migrate-standalone.mjs` chạy 0344 bằng `avi_app` ⇒ `42501`; `MIGRATE_STRICT` **tắt** ⇒ deploy **xanh giả** | trên máy này đã đóng (đủ 6 giá trị enum) |
| **BG-76** | `calculatedSummary` (`ngCount`) vẫn lấy từ `summary` khai, trong khi `overallResult` nay từ dữ liệu ⇒ **hai cột cùng hàng bất đồng** | hiện **0 hàng** biểu hiện — tiềm ẩn |
| **BG-77** | Bản vá BG-68 **phụ thuộc một lỗi có sẵn**: `measurements[] \|\| points[]` (mảng rỗng là truthy) | |
| **BG-53** | Hai nơi gọi `inferAoiOverallResult` nuôi **hai đầu vào khác nhau** | |
| **BG-54** | `presign` thiếu kiểm `pkg.machineId` ⇒ **rò chuỗi tenant** + chiếm chỗ `packageId` | nguy hiểm **ngay hôm nay**, cờ TẮT |
| **BG-55** | ✅ **ĐÃ VÁ 2026-08-31** (C-1, review lượt 8, commit `2d340335`): bỏ cổng `if (metaData.serialNumber)` ở `aoiPackageRouter.commit` — điều kiện ghi nay là "có cây hợp lệ", hội tụ theo `packageId`. Hook WIP cũng thôi gác bằng serial | lưới `aoiPackageSerialRongVanGhi.test.ts` (4 ca) + hình dạng `serialRongVanPhaiGhiDuoc_C1` trong `BANG_HINH_DANG` chạy qua cổng tích hợp SỐNG |
| ~~**BG-92**~~ | **ĐÓNG** (Khối B Task 4 `cd93d494`; neo thời gian BG-97 `c98781db`/`dff2e531`; giới hạn dạy được ở Khối C). Trước đây: **Spec-gate `evaluatePointResult` KHÔNG có điểm gọi trên đường v2** (cửa ZIP mất nó ở `df20b31c`; `submitInspectionTreeV2` chưa bao giờ có) ⇒ linh kiện ngoài giới hạn mà máy khai OK sẽ được ghi OK. Chưa nối được vì cây v2 chỉ mang `componentExtId`, còn cổng cần `pointDefId` — ánh xạ đó là dữ liệu **Khối B**. Phải đóng **CÙNG Đ-19** | `evaluatePointResult` còn **1** điểm gọi sản xuất (đường v1.x phẳng); `isPointLimitEvalEnabled()` mặc định **ON**; 294/296 gói `committed` mang hình dạng phẳng ⇒ cổng vẫn đang sống trên hình dạng sinh ra dữ liệu hôm nay |
| **BG-93** | **`audit_logs` KHÔNG có retention/partition, và vai ứng dụng KHÔNG dọn được nó.** Đo bằng `information_schema.role_table_grants` với `current_user='avi_app'`: quyền trên `audit_logs` = **`INSERT`, `SELECT`** (WORM, mig `0224`) ⇒ **không tác vụ dọn nào chạy bằng vai ứng dụng được**; không tìm thấy job retention nào. I-4 (review lượt 8) đã đóng phần "người CHƯA XÁC THỰC ghi được vào đó" và đặt trần tăng trưởng bằng trần ingest per-máy, **nhưng không đóng phần tăng trưởng dài hạn**. Lời giải phải là **partition theo thời gian + một vai riêng có `DELETE`/`DROP PARTITION`** — ⚠ **KHÔNG** cấp `DELETE` cho `avi_app` (mất tính WORM) và **KHÔNG** viết `DELETE` nào từ mã ứng dụng | `current_database()='aoi_management'`: **4 061** hàng / **2 968 kB**, tín hiệu hình dạng ingest **0** hàng. `current_database()='aoi_management_test'`: **20 853** hàng / **11 MB** (~566 byte/hàng), trong đó `ingest_shape_legacy`=**272**, `ingest_shape_v2`=**56**. Ở nhịp 1 hàng/lượt ingest THẬT và 10 000 bo/ngày ⇒ **≈5,7 MB/ngày ≈ 2 GB/năm** chỉ riêng tín hiệu đếm |
| **BG-94** | **Lưới tài liệu chỉ cưỡng chế được phần ĐO ĐƯỢC của bất biến "mọi khẳng định hành vi trong tài liệu phải đúng".** §5 của `taiLieuMetaJsonKhopHopDong.test.ts` (I-2, review lượt 8) nay canh ba thứ: tên trường suy TỪ `metaJsonSchema` SỐNG · sáu khẳng định ĐÃ BỊ MÃ BÁC BỎ phải vắng mặt · khẳng định về nguồn cuộn verdict phải CÓ MẶT. **Một câu SAI MỚI, chưa ai gặp, vẫn lọt** — vì "mọi khẳng định đúng" không máy kiểm được. Hướng đóng thật: sinh phần mô tả hợp đồng TỪ `metaJsonSchema` (một nguồn), thay vì viết tay rồi canh bằng mẫu | §5 hiện có **6** mẫu khẳng định-đã-bác-bỏ, mỗi mẫu có ca **chống-bộ-dò-không-khớp-gì** (phải bắt được ĐÚNG câu tài liệu từng dạy) và **2** đối chứng ÂM (bộ lọc `listPackages.overallResult` 3 giá trị · hợp đồng PHẲNG v1.x còn nhận `result:"NTF"`, đo bằng `machineDataContractV1.safeParse`) |
| **BG-95** | **Migration TÁI CHẠY ĐƯỢC có thể PHỤC SINH ràng buộc mà migration SAU đã cố tình bỏ.** Task 5 Khối B đo: chạy lại `0338` (idempotent) SAU `0347` phục sinh `uq_product_surfaces_model_name` ở cả hai DB ⇒ khôi phục đúng lỗ hai máy ghi đè nhau — **im lặng, mọi lưới vẫn xanh** vì không lưới nào chạy migration hai lần. Đã guard `0338` + lưới canh; **phần còn lại chưa quét**. Cần census BẤT BIẾN: *không migration tái chạy được nào tạo ràng buộc mà migration sau nó đã bỏ* | Bề mặt đo: **240** migration có `IF NOT EXISTS`, **76** trong đó tạo UNIQUE index/constraint (`drizzle/*.sql`) |
| **BG-104** | **`measurementPointImport.ts` có gate `touchesLimits` RIÊNG, thiếu 9/18 cột, `unit` gán vô điều kiện ⇒ bulk-import LÁCH hàng đợi duyệt trên sản phẩm sống.** Review Task 7 Khối C tìm ra (có sẵn trước Task 7). Đang được Task 8 đóng bằng cách dùng CHUNG hàm suy từ `POINT_LIMIT_SPEC`; census §3 (`01c459fd`) đã allowlist tạm với test permanent **tự đỏ** khi sửa xong ⇒ phải gỡ cả allowlist lẫn test | Grep co-occur 18 tên cột: `measurementPointImport.ts` **9/16**, `productPackageService.ts` 16/16 (đã sửa `01c459fd`), `machineApiRouters.ts:2740-2769` 16/16 (đã sửa `01c459fd`) |
| **BG-107** | **BỐN tệp client CHÉP TAY cột giới hạn ngoài tầm census:** `client/src/pages/ProductModels.tsx` (16/16), `client/src/components/productModels/types.ts` (16/16, ⚠ đường dẫn reviewer khai `pages/` SAI — đo lại), `client/src/components/productModels/PointDetailsForm.tsx` (16/16), `client/src/components/BulkImportDialog.tsx` (10/18, bắt được sau khi vòng sửa 2 nới quét — R-KC-7). ⚠ **R-KC-8:** tệp thứ tư là bảng ánh xạ HEADER Excel/CSV (alias EN/VI), cùng lớp rủi ro nhưng di trú cần thiết kế alias-map (không có trong spec) — Task 14 chỉ di trú 3 tệp đơn; BulkImportDialog để riêng, xem BG-110. Re-review Task 7 Khối C đo được — §3 census (`01c459fd`) chỉ quét `server/` (`GOC_QUET` cứng), không bao giờ chạm `shared/`/`client/src/`. Vòng sửa 2 Task 7 nới gốc quét + allowlist tạm với test tự-đỏ. **Di trú 3 tệp sang import `POINT_LIMIT_SPEC` ở Task 14** (tách shell `ProductModels.tsx` 3 546 dòng — đúng lúc đang mổ tệp) | `shared/` dùng chung server–client (`rollupVerdict.ts` đã được client import) ⇒ không cần cầu nối. Task 10/11 bị cấm chép tay ngay từ đầu |
| **BG-108** | **Bất biến "mọi ghi cột giới hạn đều qua hai hàm DB có snapshot version" ràng buộc bởi QUY ƯỚC, không bởi kiến trúc.** Review Task 8 Khối C grep 14 điểm `.update(measurementPointDefs)` toàn `server/` — hôm nay 0 điểm chạm `APPROVAL_LIMIT_FIELDS` ngoài `updateMeasurementPointDef`/`updateMeasurementPointLimitsBatch`. Nhưng điểm gọi thứ 15 `.set({lowerLimit})` thẳng vẫn lách cả gate lẫn lịch sử ⇒ bo cũ chấm theo limit mới, im lặng (đúng lỗ BG-97 cảnh báo). Cần census BẤT BIẾN: quét mọi `.update(measurementPointDefs)`, đọc SET clause, giao với `APPROVAL_LIMIT_FIELDS` ⇒ chỉ được xuất hiện trong hai hàm đó | 4 điểm gọi gate hiện có (`update` · `setLimitsBatch` · `dataRouters.importMeasurementPoints` · AI Copilot `update_measurement_point`) đều đủ gate + version, 48/48 |
| **BG-111** | **[ĐÓNG 2026-09-05, Lô 7 — Mục 1 `099a5c01` (migration DROP NOT NULL proposedLsl/Usl) · Mục 2 `a0e39f8d` (hợp đồng `request` mở rộng `deXuat` đủ bộ APPROVAL_LIMIT_FIELDS + `decideApproval` áp qua updateMeasurementPointDef, tương thích 176 dev + 30 test hàng `requested` tồn kho) · Mục 3 `2e29b0e1` (nút "Gửi yêu cầu duyệt" trong Alert cần-duyệt, ComponentLimitsDialog) · Mục 4 (commit này — màn duyệt hiển thị đủ bộ suggestion.deXuat + tương thích hàng cũ)]** **KHÔNG có cầu nối tự động từ sửa-trực-tiếp sang hàng đợi duyệt.** Task 11 Khối C đo: `assertThresholdEditAllowed` chỉ ném FORBIDDEN trên sản phẩm sống, **không bao giờ ghi** `threshold_approvals`. Mutation duyệt thật `thresholdApproval.request` có hợp đồng **hẹp hơn** (1 điểm, LSL+USL bắt buộc, không `unit`/`height*`) ⇒ tự bắc cầu sẽ **rơi dữ liệu im lặng**. Dialog dạy giới hạn (`0d570915`) chọn báo thật *"chưa lưu — cần duyệt"* thay vì khai một lượt gửi không xảy ra. Kỹ sư bị chặn phải tự điều hướng sang Threshold Approvals. Đây là **quyết định kiến trúc** (mở rộng hợp đồng duyệt để nhận batch + đủ trường spec, hay giữ hai đường tách), không phải vá — Lô 7 chọn MỞ RỘNG hợp đồng (không giữ hai đường tách): `deXuat: Record<field, string|null>` thay LSL/USL bắt buộc, cầu là NÚT NGƯỜI BẤM (không auto-ghi ngầm — `assertThresholdEditAllowed` vẫn chặn y nguyên) | Gate + version đủ ở cả 4 điểm gọi (BG-108); chỉ thiếu đường đi tiếp cho người dùng khi bị chặn — nay đã có (nút "Gửi yêu cầu duyệt") |
| **BG-116** ⛔ | **[HỆ ĐÃ SẴN — CHỜ BÊN MÁY TÍCH HỢP (2026-09-05, Lô 8), KHÔNG PHẢI ĐÓNG HẲN.** Chủ dự án chốt hướng (a): máy upload qua presign, `05/09`. Mục 1 (server) `f16b1ac3` + fix-up review (cùng lô): `machineApi.presignTemplateImage`/`.commitTemplateImage` (scope `ingest:write` — CÙNG `submitMachineTemplate`, vocabulary KHÔNG có `config:write` như mô tả gốc đoán) + `PUT /api/machine-template-image/upload/:objectKey(*)` — ghi `product_captures.templateImageUrl`/point-def `referenceImageUrl`, lọc máy qua `product_captures.machineId`, idempotent theo (hàng, sha256) qua khoá `product-models/<id>/template-<sha256>.<ext>` (KHÔNG migration, KHÔNG cột mới). ★★★ **Tự-review bắt lỗ nhập nhằng THẬT** (không phải giả định): `captureExtId` là GUID DO MÁY CẤP — KHÔNG duy nhất toàn hệ (một máy dạy CÂY CLONE cho hai sản phẩm mang CÙNG bộ GUID, đúng cảnh báo docblock `traPointDefCapComponent` đã ghi từ Khối C Task 7 nhưng lượt đầu Lô 8 bỏ qua) — đo được THẬT khi chạy CÙNG batch với ba db test khác của Khối B (chia sẻ máy có sẵn + mẫu máy thật). Vá bằng thêm `productModelCode` TUỲ CHỌN NHƯNG BẮT BUỘC KHI BIẾT ở cả hai cửa — không khai mà nhập nhằng ⇒ `BAD_REQUEST` rõ ràng, không đoán bừa một trong hai sản phẩm. Mục 2 (client) `4c030600`: `phanLoaiNguonAnhTemplate` — canvas chỉ load khi URL là `/uploads/`/http(s)/data:, còn đường dẫn máy còn nguyên thì hiện thông điệp trung thực thay vì nền câm mà ROI vẽ đè lên. Mục 3 (tài liệu): `docs/CSHARP_CLIENT_UPLOAD_GUIDE.md` + `MachineSection.tsx` thêm trình tự đẩy-cây → presign ảnh → PUT → commit, kèm `productModelCode`, đánh dấu TÙY CHỌN.** **CÒN MỞ: 0 máy thật đã tích hợp cửa mới** — hệ mở sẵn đường nhận, nhưng không đo được "máy có dùng nó không" tới khi bên vendor cập nhật Agent .NET. Máy CŨ (chưa nâng cấp) không upload ảnh vẫn hoạt động y hệt hôm nay — hợp đồng đẩy cây `submitMachineTemplate`/`machineTemplateContract` không đổi] | Mẫu thật: 16/16 `templateImagePath` đều là đường dẫn `D:/`. Db test 9/9 xanh (RED xác nhận bằng đột biến thật ở 2 điểm gốc: idempotent + sai-máy; mệnh đề 8 mới đo trực tiếp ca nhập nhằng captureExtId bằng cây clone thật, chạy ổn định 4/4 lượt cùng batch với ba file Khối B khác), unit test client 9/9 xanh (RED xác nhận bằng đột biến thật) |
| **BG-118** ⛔ | **[VÒNG SỬA 9: im lặng ĐÓNG, hạ oan MỞ — cờ chờ chủ dự án; bật có điều kiện NEW-3]** `56bf3367` (cuốn từ SERVER): warning boot đo N thật + census `.env.example` + mệnh đề 3 CẢ HAI trạng thái — **TẮT: haCap 1/remark NG · BẬT: haCap 0/OK** (`aoi_management_test`). Cờ vẫn TẮT — quyết định chủ dự án. **`SPEC_GATE_SNAPSHOT_ENABLED` CHƯA TỪNG được đặt ở bất kỳ đâu** — review toàn nhánh lượt 9 grep 32 khớp: `.env` 0 dòng, 8 bản `.env.bak*/.env.sim/.env.testbak` 0 dòng, docker/compose/k8s/systemd/CI/sh/package.json 0 dòng, `.env.example:1411` chỉ chú thích ⇒ toàn bộ BG-97 (`c98781db`/`dff2e531`) **TẮT** từ ngày sinh, không warning/runbook/census. Tiền đề hạ oan đủ hôm nay (`aoi_management`: 6/16 điểm cây có giới hạn, 26 hàng version): bo v2 phát lại WAL (dead-letter 101) hoặc ZIP commit muộn chấm theo giới hạn ĐANG SỐNG. **Cổng ra mệnh đề 3 (`343f8af9`) đo với cờ TẮT** — L-4 tầng cổng ra, lỗi coordinator. Vòng sửa 9 SERVER: warning boot + census `.env.example` + mệnh đề 3 CẢ HAI trạng thái. **Bật hay không = quyết định chủ dự án** (đã hỏi 2026-09-04) | Bo XẤU đi lọt: không đường nào (reviewer grep neo 3 cửa). Bo TỐT hạ oan: CÓ, hôm nay |
| **BG-119** | **[ĐÓNG vòng 2 — NEW-3 `02676ea2` (tách chuỗi version base/biến thể) + NEW-4 `3367b4b6`; re-review vòng 2 = 0C/0I]** **[VÒNG SỬA 9: NỬA VỜI + SINH LỖI: version variant ghi vào chuỗi BASE (NEW-3, cờ bật ⇒ sai hai chiều); exclude/removeOverride 0 gate (NEW-4, bo xấu LỌT), vòng 2]** `fa2769a3`: `setOverride` qua `apDungVariantPatch` + `assertThresholdEditAllowed` + version; census đếm là đường thứ 6. **Còn hở:** snapshot-gate theo variant chưa đóng hết (SERVER khai, ngoài phạm vi). **`variant_point_overrides.patchJson` = nguồn giới hạn thứ HAI vô hình với mọi census** (`productVariantRouter.ts` `setOverride`, `z.record` tự do) → merge thẳng `gateLimits` ở `machineApiRouters` — không `assertThresholdEditAllowed`, không `measurement_point_versions`. Đây chính là "điểm gọi thứ 15" BG-108 cảnh báo — **đã tồn tại sẵn**, 0 hàng hôm nay. Vòng sửa 9 SERVER: whitelist khoá ∈ spec, qua gate + version, census đếm là đường thứ 6 | Cờ BG-118 bật cũng không tái dựng được giới hạn đã bị override không version |
| **BG-120** | **[VÒNG SỬA 9: ĐÓNG (re-review xác nhận)]** `5f191e48`: basis chấm ghi `measurement_results.remark` `[SG:…;v=<versionId|LIVE>]` cả 3 đường v2, không thêm cột. **v2/ZIP không lưu giới hạn nào đã chấm bo** — `gateConfigVersion` chỉ ghi ở v1.x (`machineApiRouters` v1 path); `theoSnapshot/theoSong`, `mayTuMauThuan` chỉ sống trong response + `console.warn` ⇒ cờ BG-118 bật cũng không audit được sau. Vòng sửa 9 SERVER: ghi basis vào `measurement_results.remark` `[SG:…;v=<versionId|LIVE>]`, không thêm cột (hypertable nén) | Review lượt 9 I-4 |
| **BG-121** | **[ĐÓNG nốt vòng 2 `64b3dbd0` — BG-96 AST hợp nhất + BG-98 AST demMauThuan + BG-99 quét dòng]** **[VÒNG SỬA 9: 2/4 ĐÓNG (BG-99, §3 ngưỡng 4 đo độc lập đúng); BG-96/98 còn lách; limitRangeGateCensus mới tự thoả chiều khác, vòng 2]** `01ac529b`: BG-99/96 sang AST; BG-98 đòi giá trị không-`undefined` + quét `server/services/**`; §3 ngưỡng **4** (=arity `CapGioiHan`; "≤3" gây 10 dương tính giả) + import phải được dùng. **BỐN census mới của Khối C đều TỰ THOẢ** (review lượt 9 §4, đột biến mô tả): BG-99 mù `new Date(iso)` khi tham số không tên `completedAt/startedAt/inspectionTime` · BG-98 `includes("demMauThuan")` chấp nhận `demMauThuan: undefined`, và `server/services/**` ngoài quét · §3 tệp đã-credit thêm khối chép tay ⇒ xanh, tệp mới + 1 import KHÔNG DÙNG ⇒ xanh · BG-96 `* 60 * 1000` lách regex `*\s*60000`. **L-1 lần thứ 9–12.** Chỉ `phamViDocCensus#GHIM` đứng. Vòng sửa 9 SERVER: AST cho BG-96/99, đòi giá trị không-`undefined` + quét `services/` cho BG-98, co-occur ≤3 trong tệp credit + import phải được dùng cho §3 | Khuôn AST sẵn: `cuaIngestScan.ts` |
| **BG-122** | **[ĐÓNG vòng 2 `7edb1820` — `F` neo TÊN thay chỉ số mảng]** **[VÒNG SỬA 9: ĐÓNG nhưng SINH LỖI: FIELD_* neo CHỈ SỐ mảng (NEW-2) — đổi thứ tự spec ⇒ ghi chéo câm, vòng 2]** `56bf3367`: CLIENT di trú thật 4 khối `ProductModels.tsx` → `FIELD_*` hằng từ spec, co-occur 18→4/18 (4 còn lại ngoài khối, đối chiếu từng dòng); ngưỡng 4 làm tệp tự sạch, không cần allowlist. **Quy trình: cơ chế tự-hết-hạn bị đánh bại bởi chính người được giao.** Task 14 (`86bdf1fd`) gỡ interface trùng trong `ProductModels.tsx`, rồi **xoá** dòng `NO_DA_BIET` + test (c) BG-107 với lời khai "đã di trú" — trong khi **4 khối** chép tay 18 cột còn nguyên. Tệp thoát census chỉ nhờ credit bắc cầu cấp-file (BG-115). Review 14 bắt 1/4. ⇒ Khuôn: test (c) chỉ được xoá khi **chính nó đỏ** (đo, không khai); reviewer phải chạy census với allowlist tạm bỏ. Vòng sửa 9 CLIENT di trú thật 4 khối | L-4 lần 13 (implementer) |
| **BG-123** | **[ĐÓNG 2026-09-05 `82ced43b` — server: 20 field giới hạn (SUY từ `APPROVAL_LIMIT_FIELDS`) nhận `.nullable()`, null ⇒ SET NULL + snapshot + bump version + qua cửa duyệt; phát hiện thêm và vá luôn lỗi merge `??` ở `measurementPoint.update` từng âm thầm phục hồi giá trị cũ khi xoá; UI nút Xoá client CHƯA làm — vẫn là nợ mở]** **Không xoá được giới hạn về NULL qua UI/API** — `measurementPoint.update` và `setLimitsBatch` (`fc232773`) khai mọi cột giới hạn `z.string().optional()`, **không** `.nullable()` ⇒ client không có cách gửi "xoá". Vòng sửa 9 CLIENT (M-4) dừng đúng brief, không tự sửa server. Đóng: server thêm `.nullable()` + đường ghi NULL đi qua cùng gate/version (không UPDATE thẳng); rồi client thêm nút Xoá từng trường. ⚠ Xoá giới hạn = đổi giới hạn ⇒ phải snapshot version như mọi lần ghi (BG-97) | Review lượt 9 M-4; đo chữ ký thật `productRouters.ts` |
| **BG-134** | **Lớp lỗi "khóa tồn tại, giá trị vô nghĩa" nằm ngoài MỌI cổng i18n hiện có** — nghiệm thu mắt 05/09 bắt block `packages.*` là placeholder key-echo Ở CẢ BA locale từ ngày dựng trang (`"title": "Title"/"Tiêu đề"/"标题"`, `"uploadQueueTitle": "Upload queue title"`…); i18n:check chỉ so khóa-với-khóa, viStringCoverage chỉ bắt chuỗi trần ⇒ cả hai mù. Đã dọn block này (091dab5f: 32/36/34 khóa) + khóa `packages.uploading` thiếu (labelKey lưu-như-dữ-liệu nên i18n:check cũng mù — khuôn F12). Đóng bền = census heuristic `value ≈ humanize(key)` (vd "uploadQueueTitle" ↔ "Upload queue title") quét cả 3 locale, ngưỡng + miễn trừ có lý do; và mở rộng bộ dò khóa-lưu-như-dữ-liệu cho labelKey | Phát hiện qua ảnh chụp tự xem — không cổng nào kêu suốt đời trang |
| **BG-131** | **Hai mô hình quyền quản trị song song trong MỘT tab:** `integrityRouter.summary/runNow` dùng `adminProcedure` (đòi `role==='admin'` đúng chữ) trong khi dead-letter (Lô 4) dùng `protectedProcedure`+`requirePermission("admin_system","canView")` (cấp được cho non-admin qua bảng permissions) — scoped-admin thấy nửa tab, nửa kia 403 (Lô 4 đã vá TRUNG THỰC hóa: hiển thị "không có quyền" thay vì empty-state giả, `eab58a01`). Đóng thật = quyết định hợp nhất: nâng integrityRouter sang nhóm `admin_system` hay giữ hai tầng — quyết định phân quyền, cần chủ dự án | Review Lô 4, 05/09 |
| **BG-132** | **[ĐÓNG 2026-09-05, Lô 6 Mục 2 — quyết định chủ dự án: (A) + OR-hai-quyền]** Ruling (A) Khối D ("hub một cổng `analytics_oee`") mới cài ở ĐƯỜNG ĐỌC client, CHƯA cài ở ĐƯỜNG GHI server. Lô 5 Mục 1 đo bằng user `role:'operator'` THẬT (seed bảng `permissions` — admin bypass `checkPermission`, mọi phép đo trước đó dùng admin chứng minh 0): `layoutRouter.create/update/delete/add\|update\|removeMachinePosition` (6 mutation) đều gate `settings_factory`, KHÔNG PHẢI `analytics_oee`. Người có `analytics_oee` (không có `settings_factory`) BỊ CHẶN ở mọi mutation ghi bố cục — client-route (`RouteGuard` của `/digital-twin`) và server-gate LỆCH NHAU. Chủ dự án chốt 2026-09-05: cài ruling (A) xuống server CHO CẢ đường ghi, nhưng OR VỚI `settings_factory` (KHÔNG thay hẳn) để người dùng `settings_factory` cũ không mất quyền — chống hồi quy RÀNG BUỘC bởi ruling. Đóng bằng middleware dùng chung mới `requireAnyPermission` (`server/_core/accessControl.ts`, sinh cùng Lô 6 Mục 1) — qua nếu BẤT KỲ cặp {module,action} nào `checkPermission` true, ném CÙNG hình dạng appError với `requirePermission` (giữ tương thích từ điển i18n). `layoutRouters.ts` 6 mutation nay `requireAnyPermission([{analytics_oee,<action>},{settings_factory,<action>}])`. **RỦI RO ĐÃ NHẬN** (từ ruling (A) gốc, chủ dự án chấp nhận khi chốt): người có `analytics_oee` (vai phân tích/xem OEE, không có `settings_factory`) nay SỬA ĐƯỢC bố cục xưởng. Riêng: `listByWorkshop`/`getById` là `protectedProcedure` TRẦN, không gate quyền nào (có từ trước Khối D, không phải hệ quả ruling A, KHÔNG đổi ở Lô 6) — bất kỳ ai đăng nhập đều đọc được bố cục | `requireAnyPermissionKhoiC.db.test.ts` (Lô 6 Mục 1, 4/4 xanh DB thật — chỉ quyền 1/chỉ quyền 2/không quyền nào/admin bypass) + `layoutRoutersPermissionKhoiD.db.test.ts` (Lô 6 Mục 2, RED→GREEN nguyên văn xác nhận bằng đối chiếu nội dung router trước/sau: ca "chỉ analytics_oee" LẬT từ FORBIDDEN sang ĐƯỢC PHÉP, ca "chỉ settings_factory" GIỮ ĐƯỢC PHÉP — chống hồi quy, ca "không quyền nào" GIỮ FORBIDDEN — 5/5 xanh) |
| **BG-133** | **5 nút "`/layout?workshopId=X`"/"`/layout?factoryId=X`" có sẵn từ trước Khối D (`CorporateLayout.tsx` ~710/~1156/~1183, `CorporateManagement.tsx` ~668/~870) mất ngữ cảnh khi bấm.** `<Route path="/layout"><Redirect to="/digital-twin?tab=layout" /></Route>` (Khối D Task 1, `a86e7017`) là đích CỐ ĐỊNH — bỏ qua mọi query string đính kèm. Không phải "một lối vào rồi từ chối" (không bị chặn quyền), mà MẤT NGỮ CẢNH ÂM THẦM: bấm "xem bố cục xưởng X" từ màn tập đoàn hạ cánh ở hub layout KHÔNG có xưởng nào chọn sẵn — không lỗi hiển thị, chỉ lặng lẽ sai. Phát hiện khi đo phạm vi Lô 5 Mục 3 (grep `/layout?workshopId=` toàn client), ngoài 3 mục brief giao nên KHÔNG tự vá. Đóng: đổi 5 lời gọi sang `/digital-twin?tab=layout&workshopId=X` (hoặc `&factoryId=`) thẳng, bỏ qua bước redirect trung gian — `LayoutContent` tự đọc `useSearch()` trên URL thật (đã đọc code xác nhận không cần đổi `Layout.tsx`/`TwinHub.tsx`) | Lô 5 Mục 3, 05/09 — grep `setLocation(\`/layout?` trong `client/src/pages/*.tsx` |
| **BG-129** | **Client KHÔNG có hạ tầng render-test** (`environment: "node"`, 0 jsdom, 0 @testing-library — grep 0 `render()` trong mọi test). Hệ quả: mọi lời hứa hành vi UI (dialog-không-đóng-khi-lỗi, hiển-thị-xung-đột, focus/aria) chỉ kiểm được bằng ĐỌC MÃ hoặc Playwright nặng — review Lô 2 (05/09) bắt đúng ca này ở ComponentLimitsDialog onError. Đóng bằng: quyết định hạ tầng (thêm jsdom + testing-library cho `*.unit.test.tsx`) — một lần, mở cả lớp test | Chi phí: 1 devDep + config vitest; giá trị: lớp lời-hứa-UI hết phải tin bằng mắt |
| **BG-130** | **`setLimitsBatch` không có optimistic-lock** (chỉ `measurementPoint.update` có `expectedUpdatedAt`/MP_STALE_WRITE) — batch dạy/xóa hàng loạt là last-write-wins dưới `FOR UPDATE` row-lock. **CHẤP NHẬN THIẾT KẾ có điều kiện** (Lô 2, 05/09): mọi ghi đều có version history + qua cửa duyệt ngưỡng nên mất-cập-nhật khôi phục được; NHƯNG khi ≥2 kỹ sư cùng dạy một capture, người sau đè người trước im lặng. Nâng cấp khi có ca thật: thêm expectedUpdatedAt per-item | Ruling trong `.superpowers/sdd/2026-09-05-aoi-khoi-c-no-con-mo/progress.md` |
| **BG-124** | **[ĐÓNG 2026-09-05, cơ chế (b): commits `73eec22d`+`19f253bf`+`1f2501bf` — pre-commit hook DANH SÁCH TRẮNG `next-index-*` (chỉ pathspec-commit dựng index tạm dạng này); đo 4 ca: trần=chặn · `-a`=chặn (lỗ v1 `index.lock` do phiên -17 đo được — bài học: cơ chế an toàn phải hỏng theo hướng ĐÓNG) · pathspec=qua · `CHO_PHEP_COMMIT_TRAN=1`/`--no-verify`=lối thoát người thật. Kèm gỡ bẫy `core.hooksPath` trỏ clone cũ `c:/Apps` (di tích 29/06) làm MỌI hook câm — chính nó để lọt commit đo-lường `73eec22d "thu tran"`; installer `scripts/hooks/cai-hook.sh` nay từ chối chạy khi hooksPath lệch]** **Hai tai nạn git trong một ngày trên nhánh chung không worktree** — `97d518fc` (Task 6 stash+commit trần, R-KC-4) và `56bf3367` (CLIENT vòng sửa 9 `add -- <tệp>` đúng nhưng `commit -m` không `--`, cuốn 5 tệp SERVER đang stage, R-KC-11). Cả hai đã có luật pathspec trong brief — **luật trong văn bản không đủ**. Đóng bằng cơ chế: (a) mỗi agent một `git worktree` riêng (đã nêu R-KC-4, chưa làm vì `git worktree` từng TREO trên máy này — memory ai-sprint5); hoặc (b) pre-commit hook từ chối `git commit` khi index chứa tệp ngoài pathspec argv (đọc `GIT_INDEX_FILE` vs `git diff --cached --name-only`); hoặc (c) mỗi agent commit qua script `commit-pathspec.sh` bắt buộc `--`. Không mất dữ liệu ở cả hai lần nhờ agent khai ngay + không reset | Chi phí mỗi lần: một lượt đo + dựng lại + báo cáo ~30 phút; lần 3 có thể không may mắn thế |
| **BG-125** | **[ĐÓNG vòng 2 `3367b4b6` — exclude/removeOverride qua cửa duyệt ngưỡng + ghi version]** **`productVariantRouter.exclude` / `removeOverride` KHÔNG qua gate, KHÔNG version** — `exclude` tắt hẳn cổng chấm cho điểm đó ⇒ **bo XẤU đi lọt** (đường đầu tiên của Khối C, re-review vòng sửa 9 NEW-4). `removeOverride` xoá giới hạn hiệu lực không để lại lịch sử ⇒ cờ BG-118 bật cũng không tái dựng. Vòng sửa 2 | 0 hàng `variant_point_overrides` hôm nay — ngủ, nhưng là lỗ thật |
| **BG-126** | **[ĐÓNG 2026-09-05 `99034e1f` — `assertChangeReasonKhongGiaTienToBienThe` (dùng CHÍNH `RE_TIEN_TO_VERSION_BIEN_THE`) chặn Ở INPUT cả 4 điểm gọi thật (census đo lại ra 4, không phải 3 như mô tả gốc: thêm `revertPointsConfigToVersion.reason` + `thresholdApprovalRouter.revert.comment`); APP_CODE mới + i18n 3 locale]** **`changeReason` vừa là văn bản người gõ, vừa là DẤU HIỆU CẤU TRÚC quyết định việc chấm bo.** Sau `02676ea2` (NEW-3), `RE_TIEN_TO_VERSION_BIEN_THE = /^\[VARIANT:\d+\]/` phân biệt chuỗi version biến thể khỏi base — nhưng `changeReason: z.string().max(500)` ở **3** input router ghi **nguyên văn, 0 sanitize** (`product.ts:2002/2147/2396`). Người dùng gõ lý do bắt đầu `[VARIANT:12] …` ⇒ snapshot BASE đó **vô hình với cổng** ⇒ cờ BG-118 BẬT có thể **hạ oan**. Sửa rẻ: bảng `measurement_point_versions` **không** phải hypertable ⇒ thêm cột `variantId` an toàn, thôi mã hoá vào văn bản tự do | Re-review vòng 2 Khối C, NEW-9. Chiều sai = hạ oan, không lọt |
| **BG-127** | **[ĐÓNG 2026-09-05 `6ab522b8` (v2, sau `19cc03a5`/v1 bị review bắt Critical) — `scripts/kiem-di-san-ban.mjs`: v1 lọc `d."variantId" IS NOT NULL` SAI QUẦN THỂ (điểm variant tự thêm, trong khi `recordVariantOverrideVersion` LUÔN ghi vào điểm BASE `variantId IS NULL`) ⇒ luôn trả 0 bất kể sự thật — "cổng xanh vì mù". v2 sửa WHERE đúng (đo lại bằng git history quanh `02676ea2`/`3367b4b6`, không tin danh sách đoán) + một bug JS-escape riêng bắt được nhờ chính lưới mới viết (`scripts/kiem-di-san-ban.test.ts`, 4/4 xanh) + thêm cảnh báo phụ cho literal lạ. Số v2 chạy thật: `aoi_management`=0, `aoi_management_test`=0 (current_database() in mọi dòng) — khớp đối chiếu độc lập của phiên -46]** **Bản vá NEW-3 KHÔNG hồi tố — 0 migration/backfill.** Hàng version biến thể ghi TRƯỚC `02676ea2` không có tiền tố `[VARIANT:…]` ⇒ vẫn lẫn vào chuỗi base. Hai DB ở máy này = **0 hàng bẩn** (đã đo), nhưng **DB khác cần một câu SQL tiên quyết** trước khi bật cờ BG-118 — câu `di_san_ban` ghi trong `khoic-review9-report.md`. ⇒ Ràng buộc triển khai: chạy câu đó, phải = 0, rồi mới bật cờ | Re-review vòng 2 Khối C, NEW-10 |
| **BG-128** | **[ĐÓNG 2026-09-05 `aa72a0ee` — lưới `server/db/lienKetBoTrongBienThe.db.test.ts` đo cột `productPointsConfigVersion IS NULL` + hành vi `revertPointsConfigToVersion` thật (canary); đột biến chạy tay xác nhận ĐỎ đúng dự đoán rồi hoàn tác]** **Đường revert an toàn nhờ một liên kết CHƯA KHAI.** Hàng biến thể có `productPointsConfigVersion = null` vì lý do khác (không phải vì ai thiết kế thế) ⇒ revert hiện không kéo giới hạn biến thể đè lên điểm base. Ai "cải tiến" bằng cách đóng dấu version cho hàng biến thể sẽ **mở** đường đó. ⇒ Khai bất biến này thành lưới trước khi có người vô tình phá | Re-review vòng 2 Khối C, NEW-11 |
| ~~**BG-96**~~ | **ĐÓNG** (`aedd3096`→`2f37e9d2`, phiên -91 Task 1-3; siết bất biến ở `dff2e531`). Header và cây lệch **đúng 7 giờ** trong cùng request: `inspectionTime` dịch "fake UTC" (`getTimezoneOffset`) còn `startedAt` cấp cây ghi thô. Đóng bằng quy ước **UTC thật** sửa tại gốc + helper `docGioMay` (chuỗi trần = UTC) áp mọi điểm ingest + census cấm tái sinh | Trước: 0/435 hàng capture có `startedAt` nên chưa nổ. Sau: `getTimezoneOffset` = **0** điểm gọi ở đường ghi |
| ~~**BG-97**~~ | **ĐÓNG** (`c98781db` + neo đổi ở `dff2e531`). Đường v2 từng chấm theo giới hạn ĐANG SỐNG (v1.x có snapshot-gate + variant override, v2 không) ⇒ bo trong WAL bị hạ oan. Đóng bằng tái dùng NGUYÊN VĂN `resolveGateLimitsForBoard`, nguồn `measurement_point_versions`, neo **mốc-nhận-server** (trực tiếp=`now`, WAL=`enqueuedAt`, ZIP=`pkg.createdAt`) — tất định qua phát lại (review đo: `ProcessFn` 1 điểm gọi, luôn truyền meta). Variant override **không khoá được ở v2** (hợp đồng 0 trường variant) ⇒ cầu chì đỏ-được thay vì mã chết (Task 6 `97d518fc`) | ⚠ Cờ snapshot v2 **mặc định TẮT** — ngày Khối C bơm giới hạn PHẢI bật, nếu không v2 vẫn hạ oan (mệnh đề 7B đo đúng điều đó) |
| ~~**BG-98**~~ | **ĐÓNG** (`e931ee86` + `cf6512b2`). Máy gửi `lowerLimit`/`upperLimit` kèm 48/48 kết quả mà hệ vứt đi. Đóng bằng cổng "máy tự mâu thuẫn" `mayTuMauThuan.ts` — đọc `c.result`/`c.value` THÔ, **0 import** từ spec-gate/point_defs (tách bạch QĐ-8, đo: cùng bo spec-gate 1/17 vs tự-mâu-thuẫn 2/17). Nối cả 3 đường (trực tiếp/WAL/ZIP) + census BẤT BIẾN mọi lời gọi `dichCayKetQua(` phải bơm `demMauThuan` — chống câm cửa ZIP lần thứ tư | Chỉ đếm, không đổi verdict/remark. Chưa có màn hiển thị ⇒ tín hiệu còn chết ở tầng UI |
| ~~**BG-99**~~ | **ĐÓNG** (`dff2e531`). BG-96 từng đóng theo KHUÔN (`getTimezoneOffset`) chưa đóng theo BẤT BIẾN (chuỗi trần vẫn đọc theo TZ hệ điều hành ở `toDateOrUndefined` cấp cây). Đóng bằng `docGioMay` áp mọi điểm ingest + census bất biến mới + lưới chuỗi-trần | Review đo: drizzle TYPED nối `+0000`, `postgres-js` RAW theo TZ process ⇒ mọi phép đo thời gian phải qua typed hoặc `to_char` — GOTCHA ghi vào mọi brief |
| ~~**BG-57**~~ | **ĐÓNG (2026-09-05, Lô 3 Mục 1+2, `adc8cf0d`+`2fc95fca`) — a+b đủ.** (a) `scripts/dem-may-hinh-dang-phang.mjs` GROUP BY `audit_logs.action IN ('ingest_shape_legacy','ingest_shape_v2')` trong cửa sổ `--days`, in bảng + `current_database()` mọi output (Đ-28) + tổng đối chiếu độc lập; câu hỏi *"còn bao nhiêu máy gửi hình dạng phẳng?"* nay **trả lời được từ mã**, chạy thật. (b) Action MỚI `ingest_shape_legacy_rejected` (đăng ký `AUDIT_ACTIONS` + census `dangKyTinHieuHinhDangIngestBg89.test.ts` §E) — lượt TỪ CHỐI (cờ BẬT) giờ ĐẾM ĐƯỢC riêng, khác `ingest_shape_legacy` (chỉ đếm lượt NHẬN). Điểm ghi thật nằm ở `aoiPackageRouter.commit` (BG-39 gđ2, Mục 3 cùng lô) — đường v1 trực tiếp vẫn từ chối TRƯỚC xác thực (`.input()`, quyết định I-4 giữ nguyên, không đếm ở đó) | Chạy thật 2 DB (2026-09-05): `aoi_management` — **0 hàng tín hiệu** (chưa có ingest thật qua BG-89 ở DB này). `aoi_management_test` — **1012 legacy / 1095 v2** trên **436 "máy"**, tuyệt đại đa số là máy test-harness (`BG89-*`, `W7A-INLINE-*`), không phải máy sản xuất thật — số liệu này KHÔNG dùng để quyết định bật cờ trên môi trường thật, chỉ chứng minh script chạy đúng cơ chế. Quyết định bật cờ vẫn thuộc chủ dự án, sau khi chạy script trên DB sản xuất thật |
| **BG-100** | Census BG-99 (`fakeUtcCensus.test.ts`) khớp theo **DÒNG** (regex `new Date(` + từ khoá cùng dòng), không phải AST/bất biến ⇒ `new Date(` đa dòng hoặc qua biến trung gian lọt. **L-1 lần thứ 7.** Hôm nay 0 hình dạng như vậy ⇒ gap phòng-tái-sinh, không phải lỗ mở | Review Task 5 Khối C |
| **BG-101** | `restoreInspectionWal()` rơi `enqueuedAt` về `Date.now()` khi dòng WAL trên đĩa hỏng/thiếu trường ⇒ sau BG-97 ảnh hưởng verdict spec-gate. Hướng **an toàn** (hạ oan bo tốt, không lọt bo xấu), chỉ khi file hỏng | Review Task 5 Khối C |
| **BG-102** | `machineApiVariantGate.test.ts` chỉ đo đường vui (`patchJson:{upperLimit}`), không gửi patch mang khoá bảo vệ qua `submitInspection` thật ở tầng router; lọc chỉ được khoá ở tầng hàm thuần `apDungVariantPatch.test.ts` | Review Task 6 Khối C |
| **BG-103** | `VARIANT_PATCH_PROTECTED_KEYS` thiếu cột lineage cùng bảng: `captureRowId`, `componentExtId`, `templateVersionId`, `machineId`. Rủi ro thấp: `gateLimits` v1 chỉ đọc cục bộ, không ghi DB; đường sync máy có whitelist `projectSyncPoint` | Review Task 6 Khối C (kế thừa) |
| **BG-105** | **Tài liệu.** `coGioiHan` (UI, Task 9) = phân loại CẤU HÌNH (≥1/18 cột spec khác NULL, trừ `unit`) ≠ `khongGioiHan` của spec-gate = phân loại THEO KẾT QUẢ ĐO (cần giới hạn LẪN trị đo khớp chiều, loại lệch đơn vị). Hai câu hỏi: "đã dạy chưa" vs "bo này chấm được không" — có thể lệch trên một bo. UI phải dùng nhãn **"đã dạy giới hạn"**, không ngụ ý cổng chấm được | Review Task 9 Khối C |
| **BG-106** | Nhánh admin toàn quyền (`idsTrongPhamVi(...) === null`) của 4 procedure `cayDayRouter` chưa test riêng; nhánh chặn-người-ngoài (rủi ro cao hơn) đã đo và xanh | Review Task 9 Khối C |
| **BG-109** | `measurementPoint.setLimitsBatch` không chặn `id` trùng trong `items[]` — kết quả cuối và lịch sử vẫn đúng (mỗi vòng SELECT FOR UPDATE lại), chỉ `result.updated` đếm trùng. Kèm: `unit` thiếu `.max(20)` zod ở cả `update` lẫn `setLimitsBatch` (kế thừa), cột `varchar(20)` ⇒ chuỗi dài rơi lỗi Postgres thô thay vì 400 | Review Task 8 Khối C |
| **BG-110** | **Lỗ SẢN PHẨM có sẵn, độc lập census:** `BulkImportDialog.tsx` chỉ ánh xạ 10/18 cột giới hạn ⇒ **8 trường 3D/GD&T** (`warpageMax`, `voidPctMax`, `offsetXMax/YMax`, `tiltMax`, `thicknessMin/Max`, `criteria`) **KHÔNG nhập được** qua bulk Excel/CSV. Re-review Task 7 vòng 2 phát hiện khi đo BulkImportDialog cho R-KC-7. Đóng bằng: mở rộng alias-map header từ `POINT_LIMIT_SPEC` (một nguồn) thay vì thêm tay 8 dòng — đồng thời đóng phần BulkImportDialog của BG-107 | Đo: 10/18 field co-occur; 8 thiếu đều là nhóm 3D (89/3 252 điểm có `heightMin/Max` — nhóm 3D có dùng thật, không phải trường chết) |
| **BG-112** | **Hai fixture máy thật lệch nhau:** 32/48 component trong `D:\SOURCES\AOIData\dashboard-sample.json` **không có** UUID tương ứng trong `template-sync-sample.json`. Cổng ra mệnh đề 3 Khối C (`343f8af9`) đo được — chúng rơi `chuaDay` im lặng, **đúng thiết kế** (máy có thể báo nhiều hơn đã dạy). Nhưng: mọi ảnh chụp/demo dùng hai mẫu này sẽ thấy 32 "Chưa dạy" **không phải vì chưa ai dạy** mà vì không có trong template. Cần bên làm máy xuất lại **cặp** mẫu đồng bộ, hoặc ghi rõ trong tài liệu tích hợp rằng template phải bao phủ mọi component kết quả sẽ gửi | Mắt xích yếu nhất của chuỗi cây dạy → giới hạn → kết quả → cổng: ba mắt xích kia hard-fail khi lệch, riêng biên ingest kết quả rơi im lặng |
| **BG-113** | **[ĐÓNG vòng 2 `7175d4f3` — gate ≤ suy CẢ NĂM cặp từ POINT_LIMIT_SPEC]** **[VÒNG SỬA 9: NỬA VỜI — gate canh 2/5 cặp; area/volume/thickness LỌT (NEW-1), vòng 2]** `fa2769a3`: refine `lower≤upper`/`heightMin≤heightMax` sau merge với giá trị hiện có, dùng chung ở `measurementPointLimitGate.ts`, áp **6** đường (5 + variant override). **Không kiểm `lowerLimit <= upperLimit` / `heightMin <= heightMax` phía SERVER** — `measurementPoint.update` lẫn `setLimitsBatch` (`fc232773`) đều nhận cặp đảo ngược và ghi thẳng ⇒ spec-gate chấm theo khoảng rỗng, mọi trị đo đều `truot`. Review Task 11 Khối C phát hiện (kế thừa, không do Task 11). Client đã chặn ở dialog (vòng sửa 1 Task 11) nhưng AI Copilot `update_measurement_point`, bulk-import và caller tRPC trực tiếp **không qua dialog** ⇒ lỗ còn mở ở 3/4 điểm gọi. Đóng ở **một chỗ**: refine zod dùng chung trong `measurementPointLimitGate.ts` (nơi đã gom `touchesApprovalLimitFields`) | 4 điểm gọi gate (BG-108) — chỉ 1 có client validation |
| **BG-114** | **Lưới client tự thoả MỘT PHẦN — repo thiếu hạ tầng render + mock-tRPC.** Test hồi quy race A→B của `ComponentLimitsDialog` (`8d3e26c5`) chứng minh logic thuần `coTheLuu()` đúng nhưng **không chạm dây nối** component (`dangTaiChiTietDon → chiTietQuery.isFetching`, `handleLuu`/nút gọi `duocLuu`). Re-review Task 11 Khối C đo: dây hiện đúng (đọc nguồn), nhưng refactor cắt dây ⇒ test **vẫn xanh**. Đây là giới hạn chung của MỌI `*.unit.test.ts` client trong repo (không riêng Task 11) — lớp "lưới xanh cho tính năng không nối" đã cắn dự án nhiều lần ở server. Đóng bằng: dựng hạ tầng `@testing-library/react` + mock tRPC một lần, rồi viết ít nhất một test render cho mỗi dialog ghi dữ liệu | Task 12 nghiệm thu ảnh bù một phần (thấy được dây nối ở tầng UI thật) nhưng không tái lập được race |
| **BG-115** | **Credit bắc cầu CẤP-FILE của census §3 che phần chép tay CÒN SÓT trong cùng tệp — hai ca đã đo.** (1) `productRouters.ts`: 3 khối zod input `measurementPoint.update` chép tay, tệp thoát vì import `measurementPointLimitGate.ts` → spec (re-review Task 7 r2). (2) `ProductModels.tsx` sau Task 14 (`86bdf1fd`): `handleSavePoint.pointData` vẫn liệt kê tay 18 field, tệp thoát vì import `types.ts` → spec. Cả hai đều là **lỗ thật** đúng lớp Task 7 sinh ra để dọn, nhưng census xanh. Đóng bằng: trong tệp đã-credit, vẫn đếm co-occur và đòi ≤ ngưỡng thấp (VD ≤3), hoặc census cấp-khối bằng AST/marker | Hôm nay 2 tệp / 4 khối; cả hai đã khai rõ trong mã, không giấu |
| **BG-117** | **Ba lệch ngủ quanh readiness ↔ cây dạy** (review Task 12 Khối C, `b387507f`): (a) `productReadinessService` lọc `isActive` trên hàng cây nhưng `cayDayRouter` (Task 9) **không** — ngủ vì chưa gì set `isActive=false` cho hàng cây; ngày có xoá mềm cây ⇒ hai màn lệch. (b) `ProductReadiness.summary.numericPoints` (chỉ phẳng) và `items[limits].counts.numericPoints` (phẳng+cây) **cùng tên khoá** trong một response, lệch im lặng — chưa client nào đọc `summary.numericPoints`. (c) `measurementPoint.update` không chặn đổi `measurementType` trên hàng có `captureRowId` ⇒ hàng rơi vào cả `numericPoints` lẫn `treeRows`. Đóng (a) bằng cùng vị từ lọc ở cả hai; (b) đổi tên khoá; (c) guard ở `update` | Cả ba đều ngủ hôm nay, đo bằng đọc mã + 8/8 ca `scoreReadiness` cũ không đổi |

### ★ GHI NỢ — có ghi chép, không chặn

**Nợ hạ tầng test:** BG-15 (**32 file** dọn dẹp WORM **no-op câm**) · BG-63 (`aoiPackageInlineGate` rò **~90 hàng**, làm trôi baseline "N gói committed") · BG-61 (chưa đo tick ở quy mô 20.000) · BG-47 (trần gắn với interval, chưa có lưới canh chéo).

**Nợ nhất quán:** BG-44 (bất đồng chỉ mục 0272 ↔ `idempotencyKey`) · BG-45 (`dungKhoaKhuTrungV2` khi `startedAt` vắng ⇒ **trùng khoá**) · BG-46 (`processStoreForward` **cùng lỗ** phân loại, bán kính nhỏ hơn) · BG-58 · BG-59 · ~~BG-60~~ ✅ **ĐÃ VÁ**: nửa `commit` ở `cc322bca` (BG-87), nửa `presign` ở I-7/review lượt 8 (mig 0346 — lời khai presign được LƯU rồi đối chiếu trên byte thật ở tuyến upload + backstop `commit`; hai tài liệu hướng máy sửa theo).

**Nợ khảo sát/chữ nghĩa:** BG-5 · BG-6 · BG-12 · BG-16 · BG-19 · BG-29 · BG-30 · BG-32 · BG-33 · BG-34 · BG-38 · BG-48 · BG-49 · BG-56 · BG-62.

**Việc lớn chưa bắt đầu:** §6 giao thức version (hai luật nâng version) · §3.6 dọn mồ côi (+ BG-5 mồ côi **cấu hình**, 94 hàng) · **join gói ảnh theo `captureId`** · **Khối C** (UI sản phẩm) · **Khối D** (gộp màn hình + hạ tầng Playwright).

---

**Cập nhật 2026-09-04 (Khối C đóng):** ghi chú *"Khối D chặn vì không có tài khoản test"* **lỗi thời**. Task 12 Khối C đã mint vé phiên bằng `createSessionToken`+`ghiSoPhien` (server, không gate NODE_ENV) và `context().addCookies` (cookie `httpOnly` — `document.cookie` bị nuốt). `playwright.config.ts` chưa có `globalSetup`/`storageState` ⇒ Khối D bắt đầu bằng đúng hai thứ đó, dùng user audit có sẵn, không tạo tài khoản.

## 4. Hai thứ cần chủ dự án quyết

**Tài khoản test cho Playwright (Khối D).** Hiện `e2e/login.spec.ts` **không thật sự đăng nhập**; không có `storageState`, không fixture, không biến `E2E_*`. Không có tài khoản thì **không nghiệm thu bằng mắt được** — mà đó là yêu cầu ban đầu.

**Duyệt thiết kế UI trước khi code (Khối C).** `ProductModels.tsx` là **3.557 dòng**. Làm lại thành bảng + dialog là thay đổi người dùng nhìn thấy; nên chốt thiết kế trước.

---

## 5. Trạng thái an toàn hôm nay — nói thẳng

**Đã đóng:** bo lỗi không còn ghi thành bo đạt trên **cả ba** đường ingest · khử trùng giữ cả khi serial rỗng · WAL không mất bo khi DB chớp nháy · một bo hỏng không chặn hàng đợi · cửa ZIP không retry vô hạn · `'dead'` là trạng thái cuối thật.

**Chưa đóng, và phải nói ra:**
- ~~**Cấp component chưa lưu được** (Đ-19)~~ **ĐÓNG 2026-09-04** — cấp component ghi được và spec-gate chấm được trên v2 **khi đã dạy giới hạn** (`343f8af9`: dat+truot 0→3). ⚠ Với sản phẩm **chưa dạy** giới hạn, cổng vẫn kết luận `chuaDay`/`khongGioiHan` (nói thật, không xanh giả) ⇒ NG-rate theo linh kiện chỉ bắn ở phần đã dạy. Bản đồ bo hết "toàn xanh giả" nhưng chưa "toàn phủ" — đó là tiến độ dạy, đo được ở tab Cây dạy.
- ~~**Dead-letter là hố không ai đọc** (BG-36)~~ **ĐÓNG-đọc 2026-09-05 (Lô 4 Mục 3)** — 101 mục/7,4 MB nay đọc được qua `aoiPackage.listDeadLetters`/`getDeadLetterDetail` + tab UI trong `AOIPackages.tsx`; `integrityScan` (đã persist sẵn từ trước, chỉ thiếu UI) cũng có panel đọc. ⚠ VẪN CHƯA ĐÓNG: đây là ĐƯỜNG ĐỌC-only — một bo dead-letter vẫn KHÔNG resubmit/replay được từ UI (việc đó ngoài phạm vi Lô 4, brief ghi rõ "bước sau"); "mất bo trong im lặng" nay đổi thành "mất bo NHÌN THẤY ĐƯỢC" — người vận hành biết để xử lý thủ công, nhưng hệ chưa tự phục hồi.
- ~~**Cửa ZIP chưa gác** (BG-39 gđ2)~~ **ĐÓNG-cờ-TẮT 2026-09-05** — `commit` nay gác hình dạng phẳng CÙNG cổng/thông điệp với đường v1 trực tiếp (xem bảng ⛔ ở trên). Cờ vẫn TẮT mặc định; ngày bật, máy cũ bị chặn ở CẢ BA đường (v1 trực tiếp, batch, ZIP) — không còn đường lách.
- ~~**Cổng chặn máy cũ không đếm được** (BG-57)~~ **ĐÓNG 2026-09-05** — `scripts/dem-may-hinh-dang-phang.mjs` trả lời được câu hỏi này từ mã, chạy thật trên 2 DB (xem bảng ⛔ ở trên). Chủ dự án cần chạy lại script trên DB **sản xuất thật** (không phải DB dev/test) trước khi quyết định bật cờ.

⇒ Thứ đang giữ hệ thống an toàn hôm nay là **một giá trị mặc định cờ vẫn TẮT** + **cửa ZIP nay đã gác cùng cổng với hai đường kia**. Việc còn lại trước khi bật cờ: chạy `dem-may-hinh-dang-phang.mjs` trên DB sản xuất thật để biết còn bao nhiêu máy cần nâng cấp.
