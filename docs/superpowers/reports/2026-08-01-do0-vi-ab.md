# Đợt 0 Task 4 — A/B chất lượng tiếng Việt (Model 1 / Model 2, ẩn danh)

Sinh lúc: 2026-07-31T23:25:08.979Z

**Không kết luận model nào hay hơn trong file này — chỉ trình bày cặp câu trả lời cạnh nhau. Chủ dự án chấm.** Bảng ánh xạ nhãn↔model thật ở file riêng (không commit).

---

## 📋 CÁCH CHẤM — đọc trước khi bắt đầu

**1. "Model 1" là CÙNG MỘT model xuyên suốt cả 4 prompt. "Model 2" cũng vậy.**
Nhãn được gán **một lần duy nhất** ở đầu phiên sinh và **không hề đảo** giữa các prompt. Nghĩa là: nếu bạn thấy Model 1 tốt hơn ở Prompt 2 và Model 2 tốt hơn ở Prompt 4, thì đó là **hai tín hiệu trái chiều về cùng hai model**, không phải hai cặp không liên quan. **Phán quyết cuối cùng là một phán quyết duy nhất cho cả hai model**, không phải bốn phán quyết rời rạc.

**2. ⚠ Chỉ có 3 cặp chấm được, không phải 4.**
**Prompt 1 (RCA) KHÔNG chấm được cho mục đích so tiếng Việt** — **cả hai model đều trả lời bằng tiếng Anh**. Đây **không phải lỗi của model**: hàm `synthesize()` trong `server/services/aiRcaCopilot.ts` nhận tham số `lang` (mặc định `"vi"`) nhưng **không hề dùng nó**; toàn bộ system/user prompt là tiếng Anh thuần, không có nhánh ngôn ngữ nào (xem "Phát hiện quan trọng" #1 ngay dưới). Ép một model trả lời tiếng Việt từ một prompt tiếng Anh 100% là phép thử sai đề.
⇒ **Cặp P1 vẫn để đây** để bạn tự thấy bằng chứng của bug đó, và nếu muốn thì so *chất lượng phân tích* (không phải chất lượng tiếng Việt). **Nhưng đừng tính nó vào phán quyết ngôn ngữ.** Trọng số thực tế: **P2 · P3 · P4**.

**3. Ba prompt còn lại đại diện đường chạy thật ở mức nào**

| Prompt | Nội dung | Mức đại diện đường chạy thật |
|---|---|---|
| P1 — RCA | phân tích nguyên nhân gốc | ⚠ **không chấm ngôn ngữ được** (cả hai tiếng Anh) + cơ chế sinh khác sản xuất (xem Ghi chú P1) |
| **P2 — Báo cáo điều hành** | KPI thật từ `gatherKpis()` sống | ✅ cao — prompt verbatim, dữ liệu thật |
| **P3 — Tóm tắt chất lượng hàng ngày** | thay cho "cố vấn ngưỡng" | ✅ cao — prompt verbatim |
| **P4 — Trợ lý tri thức** | citation thật từ kho `chunks.jsonl` | ✅ cao — prompt verbatim, truy hồi thật |

**4. Điền phiếu bên dưới.** Mỗi cặp có một ô ghi phán quyết ngay dưới bảng so sánh; phiếu tổng hợp ở đây.

### 🗳 PHIẾU CHẤM — chủ dự án điền

| Cặp | Model nào viết tiếng Việt tốt hơn? | Mức chênh | Ghi chú của bạn |
|---|---|---|---|
| **P1 — RCA** | ⬜ *không chấm được — cả hai tiếng Anh* | — | |
| **P2 — Báo cáo điều hành** | ☐ Model 1 ☐ Model 2 ☐ ngang nhau | ☐ rõ rệt ☐ nhẹ | |
| **P3 — Tóm tắt chất lượng** | ☐ Model 1 ☐ Model 2 ☐ ngang nhau | ☐ rõ rệt ☐ nhẹ | |
| **P4 — Trợ lý tri thức** | ☐ Model 1 ☐ Model 2 ☐ ngang nhau | ☐ rõ rệt ☐ nhẹ | |

**PHÁN QUYẾT TỔNG HỢP (dựa trên P2/P3/P4):**

☐ **Model 1 tốt hơn rõ rệt** ☐ **Model 2 tốt hơn rõ rệt** ☐ **Không phân biệt được rõ / khác biệt không đáng kể với công việc thật**

> Ghi chú tự do: ______________________________________________

**Phán quyết này nối thẳng vào cây quyết định §7.5 của `2026-08-01-do0-roster-survey.md`** — mỗi ô ở trên dẫn tới một nhánh roster khác nhau. ⚠ Trước khi chốt roster, đọc thêm **§7.1a** của báo cáo đó: có một hộ tiêu thụ VRAM 7,8 GB (sidecar thị giác) làm thay đổi điều kiện của **mọi** nhánh.

---

## ⚠ Phát hiện quan trọng (độc lập với việc chọn model — không phải kết luận A/B)

1. **RCA copilot sinh tiếng Anh, không phải tiếng Việt, dù `lang` mặc định "vi".** `synthesize(input, lang, ev)` (`server/services/aiRcaCopilot.ts`) nhận tham số `lang` nhưng KHÔNG hề tham chiếu nó trong thân hàm — `sys`/`userPrompt` 100% tiếng Anh, không có nhánh ngôn ngữ nào. Xác nhận bằng lượt sinh thật ở Prompt 1 bên dưới: cả 2 model đều trả lời tiếng Anh. Đây là **bug sản phẩm thật**, không liên quan tới việc chọn roster model nào thường trú — **KHÔNG vá trong khảo sát này** (phát hiện của khảo sát, sửa là việc đợt khác).
2. **"Cố vấn ngưỡng" (threshold advisor) không hề gọi LLM.** Đã grep 7 file liên quan (`aiThresholdAdvisor.ts`, `aiSetupAdvisor.ts`, `aiThresholdTuneScheduler.ts`, `aiCalibration.ts`, `aiAnomalyCalibration.ts`, `thresholdGovernanceService.ts`, `aiAutoProposer.ts`) cho pattern gọi model (`generateText|generateNarrative|generateJSON|routeInference|aiGgufEngine|aiProviderRouter`) → **0 khớp**. Toàn bộ domain này là thống kê thuần (`suggestThresholds()`) + chuỗi đa ngôn ngữ TĨNH, không phải LLM sinh — nếu quyết định roster dựa một phần vào "ảnh hưởng tới cố vấn ngưỡng", trục này **không hề bị ảnh hưởng** bởi việc đổi model 30B nào thường trú. Prompt 3 bên dưới dùng bản THAY THẾ (`aiReportGenerator.ts`) vì lý do này — xem "Ghi chú" của Prompt 3.

## Tham số sinh (giống hệt nhau cho cả 2 model, cả 4 prompt)

| Tham số | Giá trị |
|---|---|
| temperature | 0 (greedy — xem lý do trong mã script) |
| top_p | 0.9 (no-op ở temperature=0) |
| max tokens | 700 |
| seed | không hỗ trợ ở tầng `generateText()` hiện tại; temperature=0 → không cần |
| contextSize (lúc nạp model) | 8192 |
| gpuLayers | "max" (full GPU offload, cả 2 model) |

Lệnh sinh file này:
```bash
npx tsx scripts/ai-survey/vi-quality-ab.mjs > docs/superpowers/reports/2026-08-01-do0-vi-ab.md
```

---

## Prompt 1 — RCA — phân tích nguyên nhân gốc

**Nguồn:** `server/services/aiRcaCopilot.ts` — 500-513 (hàm synthesize())

**Ghi chú:** `sys` verbatim từ nguồn. PHÁT HIỆN: sys/userPrompt KHÔNG có nhánh tiếng Việt nào (tham số `lang` không được dùng trong synthesize(), dù mặc định lang="vi") — model có thể trả lời bằng tiếng Anh dù đây là luồng RCA cho người vận hành nói tiếng Việt. EVIDENCE tái dựng từ Pareto thật (gatherKpis), không gọi được gatherEvidence() (nội bộ, cần 6 nguồn phụ). ⚠ KHAI BÁO CƠ CHẾ SINH (review round 1, Important 2): sản xuất THẬT gọi `generateJSON()` (`aiRcaCopilot.ts:527-539`) — ràng buộc theo `RCA_JSON_SCHEMA` bằng GBNF grammar (chỉ cho phép field `cause`/`confidence`/`evidence`/`recommendedFix` đúng kiểu). Lượt sinh dưới đây dùng `generateText()` TỰ DO (không ràng buộc schema) — CỐ Ý, không phải sai sót: mục tiêu Task 4 là chấm CHẤT LƯỢNG VĂN XUÔI tiếng Việt, mà GBNF ép đúng khuôn JSON gần như không còn văn xuôi tự nhiên để chấm (chỉ còn field ngắn, không phải câu/đoạn). Hệ quả quan sát được: cả 2 model dưới đây đều KHÔNG khớp `RCA_JSON_SCHEMA` thật (thiếu field bắt buộc, tự đặt field lạ như `"rank"`/`"evidenceSupport"`) — vì không bị ép theo schema. Điều này áp dụng ĐỀU cho cả 2 model (không phá tính công bằng của phép so) nhưng làm P1 kém đại diện cho ĐƯỜNG CHẠY THẬT hơn 3 prompt còn lại — người đọc nên coi P1 là "cùng system+user prompt nhưng cơ chế ràng buộc đầu ra khác sản xuất", không phải "y hệt sản xuất".

### System prompt (dùng chung cho cả 2 model)

```
You are an SMT/AOI manufacturing root-cause analyst. Using ONLY the supplied evidence, produce ranked hypotheses for the defect. NEVER invent evidence. If evidence is weak, return fewer hypotheses with lower confidence. Output strictly the JSON schema. recommendedFix.kind must be WRITE (maps to a known write-tool), MANUAL (hands-on steps), or INVESTIGATE (more data needed). For WRITE, set tool + args only if you are confident.
```

### User prompt (dùng chung cho cả 2 model)

```
Defect type: Cao linh kiện C12
Machine: (unspecified)

EVIDENCE:
Pareto top defects: Cao linh kiện C12 (6, 42.9%); Điện trở shunt (2, 14.3%); Đồng phẳng BGA (2, 14.3%); Nghiêng linh kiện (2, 14.3%); Cao đầu cắm (2, 14.3%)

Known write-tools you may reference for a WRITE fix: adjust_ng_threshold, configure_inspection_param, create_ng_threshold, update_product_quality_target, set_machine_param, acknowledge_machine_alarm, create_maintenance_workorder.
Return up to 3 hypotheses ranked by confidence (0..1, descending).
```

### Cặp câu trả lời

<table>
<tr><th width="50%">Model 1</th><th width="50%">Model 2</th></tr>
<tr>
<td valign="top">

<pre style="white-space:pre-wrap">{
  "hypotheses": [
    {
      "rank": 0.9,
      "description": "Cao linh kiện C12 defect is likely caused by incorrect placement height threshold in the AOI inspection, leading to false positives or missed detection of actual height issues.",
      "recommendedFix": {
        "kind": "INVESTIGATE",
        "reason": "No direct evidence on inspection parameters; further data needed to confirm if threshold misconfiguration is responsible."
      }
    },
    {
      "rank": 0.7,
      "description": "Cao linh kiện C12 may result from inconsistent solder paste volume or print defects affecting component height post-reflow.",
      "recommendedFix": {
        "kind": "INVESTIGATE",
        "reason": "No evidence of solder paste or reflow process issues; cannot confirm root cause without additional data."
      }
    },
    {
      "rank": 0.5,
      "description": "The defect could be due to mechanical misalignment in the pick-and-place machine causing components to be placed too high.",
      "recommendedFix": {
        "kind": "INVESTIGATE",
        "reason": "No evidence of machine alignment or placement errors; requires physical inspection or machine logs."
      }
    }
  ]
}</pre>

<sub>271 token sinh ra, 1366ms</sub>

</td>
<td valign="top">

<pre style="white-space:pre-wrap">{
  "hypotheses": [
    {
      "rank": 1,
      "confidence": 0.85,
      "description": "Insufficient inspection parameters for C12 component height detection, leading to false positives in height measurements",
      "recommendedFix": {
        "kind": "WRITE",
        "tool": "configure_inspection_param",
        "args": {
          "parameter": "height_threshold",
          "component": "C12"
        }
      },
      "evidenceSupport": [
        "Cao linh kiện C12 is the top defect (6 occurrences, 42.9%)",
        "Machine parameters may not be optimized for C12 component height detection"
      ]
    },
    {
      "rank": 2,
      "confidence": 0.75,
      "description": "Incorrect NG threshold settings specifically for C12 component height measurements causing false rejects",
      "recommendedFix": {
        "kind": "WRITE",
        "tool": "create_ng_threshold",
        "args": {
          "component": "C12",
          "measurement_type": "height"
        }
      },
      "evidenceSupport": [
        "Cao linh kiện C12 is the primary defect type",
        "Pareto analysis shows this defect dominates other issues"
      ]
    },
    {
      "rank": 3,
      "confidence": 0.65,
      "description": "Potential machine calibration or sensor alignment issue affecting height measurement accuracy for C12 components",
      "recommendedFix": {
        "kind": "INVESTIGATE",
        "tool": null,
        "args": null
      },
      "evidenceSupport": [
        "Cao linh kiện C12 is the most frequent defect type",
        "No specific machine parameters mentioned in evidence",
        "Requires additional diagnostic data to confirm sensor issues"
      ]
    }
  ]
}</pre>

<sub>404 token sinh ra, 2378ms</sub>

</td>
</tr>
</table>

> ### 🗳 Phán quyết cặp P1 — RCA
>
> ⚠ **CẶP NÀY KHÔNG CHẤM TIẾNG VIỆT ĐƯỢC.** Cả hai model đều trả lời **tiếng Anh** — vì hàm `synthesize()` (`server/services/aiRcaCopilot.ts`) **bỏ qua tham số `lang`** và gửi đi một prompt tiếng Anh 100%. Đây là **bug sản phẩm, không phải lỗi model**. Cặp này để đây làm **bằng chứng của bug đó**.
>
> **KHÔNG có ô chấm ngôn ngữ cho cặp này, và KHÔNG tính vào phán quyết tổng hợp.**
>
> *(Tuỳ chọn — nếu bạn muốn so **chất lượng phân tích** thay vì chất lượng tiếng Việt:)*
> ☐ Model 1 phân tích tốt hơn  ☐ Model 2 phân tích tốt hơn  ☐ ngang nhau  ☐ bỏ qua
>
> Ghi chú: ______________________________________________________



---

## Prompt 2 — Báo cáo điều hành

**Nguồn:** `server/services/aiExecutiveReport.ts` — 318-342 (buildSystemPrompt("vi") + buildUserPrompt(kpis))

**Ghi chú:** KPI THẬT từ gatherKpis("day","vi") — totalInspections=3540, fpy=97.3%, ngRate=1.8%, 5 top defect, 0 máy rủi ro. không cảnh báo dữ liệu.

### System prompt (dùng chung cho cả 2 model)

```
Bạn là giám đốc chất lượng nhà máy AOI/AVI. Dựa trên bộ KPI THỰC được cung cấp (JSON), hãy viết một bản tóm tắt điều hành NGẮN GỌN, sắc bén cho ban lãnh đạo bằng tiếng Việt. Tập trung vào sản lượng, tỷ lệ đạt (FPY), xu hướng NG, các lỗi hàng đầu, và máy có rủi ro hỏng. Chỉ dùng số liệu trong dữ liệu, không bịa. Trả lời theo đúng định dạng được yêu cầu.
```

### User prompt (dùng chung cho cả 2 model)

```
{"instruction":"Produce: headline (1 sentence), highlights (3-5 bullets), risks (2-4 bullets), recommendations (2-4 bullets).","kpis":{"period":"day","lang":"vi","window":{"start":"2026-07-13T00:00:00.000Z","end":"2026-07-14T00:00:00.000Z"},"totalInspections":3540,"okCount":3445,"ngCount":63,"fpy":97.31638418079096,"ngRate":1.7796610169491527,"prevNgRate":2.197392923649907,"ngRateTrend":"flat","oeeQuality":97.31638418079096,"topDefects":[{"type":"Cao linh kiện C12","count":6,"percentage":42.857142857142854},{"type":"Điện trở shunt","count":2,"percentage":14.285714285714285},{"type":"Đồng phẳng BGA","count":2,"percentage":14.285714285714285},{"type":"Nghiêng linh kiện","count":2,"percentage":14.285714285714285},{"type":"Cao đầu cắm","count":2,"percentage":14.285714285714285}],"ngRateSeries":[{"t":"2026-07-13 07:00:00","ngRate":1.1922503725782414,"total":671},{"t":"2026-07-13 08:00:00","ngRate":2.366863905325444,"total":676},{"t":"2026-07-13 09:00:00","ngRate":1.4814814814814816,"total":675},{"t":"2026-07-13 10:00:00","ngRate":2.0648967551622417,"total":678},{"t":"2026-07-13 11:00:00","ngRate":1.7857142857142856,"total":840}],"pdmRiskMachines":[],"dataWarnings":[]}}
```

### Cặp câu trả lời

<table>
<tr><th width="50%">Model 1</th><th width="50%">Model 2</th></tr>
<tr>
<td valign="top">

<pre style="white-space:pre-wrap">**Headline:** Ngày 13–14/7/2026, sản lượng đạt 3.540 đơn vị với FPY 97,32%, tỷ lệ NG ổn định ở mức 1,78% nhưng có xu hướng tăng nhẹ trong ca sáng.

**Highlights:**
- Tỷ lệ đạt (FPY) duy trì ở mức cao: **97,32%**, vượt ngưỡng mục tiêu.
- Tổng số sản phẩm lỗi (NG): **63/3.540** đơn vị, tỷ lệ NG trung bình **1,78%** – giảm nhẹ so với kỳ trước (2,20%).
- 5 lỗi hàng đầu chiếm **99,99%** tổng lỗi: Cao linh kiện C12 (42,86%), Điện trở shunt, Đồng phẳng BGA, Nghiêng linh kiện và Cao đầu cắm (mỗi loại 14,29%).
- Tỷ lệ NG trong ca sáng dao động từ **1,19% đến 2,06%**, không có xu hướng tăng mạnh nhưng cần theo dõi.

**Risks:**
- Lỗi **Cao linh kiện C12** chiếm hơn 42% tổng lỗi – dấu hiệu của vấn đề cụ thể tại một vị trí hoặc quy trình lắp ráp.
- Tỷ lệ NG trong ca sáng (07:00–11:00) có biến động rõ rệt, đặc biệt tăng lên **2,37%** lúc 08:00 – nguy cơ mất kiểm soát chất lượng nếu không điều chỉnh kịp thời.
- Không có máy nào được cảnh báo rủi ro (PDM Risk Machines = []) nhưng cần giám sát kỹ các ca làm việc để phát hiện sớm bất thường.

**Recommendations:**
- Khảo sát lại quy trình lắp linh kiện C12, đặc biệt tại vị trí C12 – nguyên nhân chính gây lỗi.
- Tăng cường kiểm tra chất lượng và hiệu chỉnh máy trong ca sáng (07:00–11:00) để ổn định tỷ lệ NG.
- Phân tích sâu dữ liệu từng ca để xác định yếu tố ảnh hưởng cụ thể tại thời điểm 08:00.</pre>

<sub>491 token sinh ra, 2152ms</sub>

</td>
<td valign="top">

<pre style="white-space:pre-wrap">**Tiêu đề:**  
Báo cáo điều hành ngày 14/07: Sản lượng ổn định, FPY đạt 97,32%, tỷ lệ NG ổn định nhưng cần theo dõi các lỗi phổ biến.

**Điểm nổi bật:**  
- Tổng số kiểm tra: 3.540 sản phẩm, trong đó 63 sản phẩm không đạt (NG), tỷ lệ NG là 1,78%.  
- FPY đạt 97,32%, OEE chất lượng tương ứng.  
- Xu hướng tỷ lệ NG ổn định (flat) so với ngày trước đó (2,20%).  
- Các lỗi phổ biến nhất: "Cao linh kiện C12" (42,86%), "Điện trở shunt", "Đồng phẳng BGA", "Nghiêng linh kiện", và "Cao đầu cắm" (mỗi loại 14,29%).  

**Rủi ro:**  
- Tỷ lệ NG vẫn cao hơn so với mục tiêu, đặc biệt trong khung giờ 08:00–10:00.  
- Các lỗi chủ yếu liên quan đến độ chính xác của linh kiện và quá trình lắp đặt.  
- Không có máy nào được đánh giá là có nguy cơ hỏng cao (PDM risk machines = 0).  

**Khuyến nghị:**  
- Tăng cường giám sát quy trình kiểm tra trong khung giờ 08:00–10:00 để giảm tỷ lệ NG.  
- Kiểm tra lại thiết bị và quy trình lắp đặt linh kiện C12, do đây là lỗi phổ biến nhất.  
- Đánh giá lại quy trình kiểm tra các loại linh kiện có nguy cơ nghiêng hoặc cao đầu cắm.</pre>

<sub>390 token sinh ra, 2169ms</sub>

</td>
</tr>
</table>

> ### 🗳 Phán quyết cặp P2 — Báo cáo điều hành
>
> Nhắc lại: **Model 1 ở đây là cùng một model với Model 1 ở mọi cặp khác.**
>
> **Model nào viết tiếng Việt tốt hơn ở cặp này?**  ☐ Model 1  ☐ Model 2  ☐ ngang nhau
>
> **Mức chênh:**  ☐ rõ rệt  ☐ nhẹ
>
> **Vì sao (câu/đoạn cụ thể khiến bạn quyết như vậy):**
>
> ______________________________________________________________



---

## Prompt 3 — Tóm tắt chất lượng hàng ngày (thay "cố vấn ngưỡng" — xem note)

**Nguồn:** `server/services/aiReportGenerator.ts` — 556-560 (trong generateDailyQualitySummary())

**Ghi chú:** THAY THẾ cho "cố vấn ngưỡng" theo yêu cầu brief — đã tìm trong aiThresholdAdvisor.ts, aiSetupAdvisor.ts, aiThresholdTuneScheduler.ts, aiCalibration.ts, aiAnomalyCalibration.ts, thresholdGovernanceService.ts, aiAutoProposer.ts (grep generateText|generateNarrative|generateJSON|routeInference|aiGgufEngine|aiProviderRouter trên cả 7 file) → 0 khớp. Không có prompt LLM nào trong domain "cố vấn ngưỡng" — 100% thống kê (suggestThresholds()) + chuỗi đa ngôn ngữ TĨNH, không phải LLM sinh. Số liệu tái dùng từ gatherKpis() (P2), ghép theo đúng công thức/nhãn field của aiReportGenerator.ts:530-546.

### System prompt (dùng chung cho cả 2 model)

```
Bạn là chuyên gia chất lượng AOI. Viết tóm tắt báo cáo chất lượng hàng ngày bằng tiếng Việt.
```

### User prompt (dùng chung cho cả 2 model)

```
{"period":"2026-07-13 to 2026-07-14","total":3540,"ok":3445,"ng":63,"yieldRate":97.31638418079096,"topDefects":[{"type":"Cao linh kiện C12","count":6,"percentage":9.523809523809524},{"type":"Điện trở shunt","count":2,"percentage":3.1746031746031744},{"type":"Đồng phẳng BGA","count":2,"percentage":3.1746031746031744},{"type":"Nghiêng linh kiện","count":2,"percentage":3.1746031746031744},{"type":"Cao đầu cắm","count":2,"percentage":3.1746031746031744}],"anomalies":[]}
```

### Cặp câu trả lời

<table>
<tr><th width="50%">Model 1</th><th width="50%">Model 2</th></tr>
<tr>
<td valign="top">

<pre style="white-space:pre-wrap">**BÁO CÁO CHẤT LƯỢNG HÀNG NGÀY – AOI (13/07/2026 - 14/07/2026)**

- **Thời gian kiểm tra:** 13/07/2026 đến 14/07/2026  
- **Tổng số sản phẩm kiểm tra:** 3.540 pcs  
- **Sản phẩm đạt yêu cầu (OK):** 3.445 pcs  
- **Sản phẩm không đạt (NG):** 63 pcs  
- **Tỷ lệ thành công (Yield Rate):** 97,32%  

---

### 🔍 **Các lỗi phổ biến nhất (Top Defects):**
1. **Cao linh kiện C12** – 6 lần (9,52%)  
2. **Điện trở shunt** – 2 lần (3,17%)  
3. **Đồng phẳng BGA** – 2 lần (3,17%)  
4. **Nghiêng linh kiện** – 2 lần (3,17%)  
5. **Cao đầu cắm** – 2 lần (3,17%)  

---

### 📌 **Nhận xét &amp; Đề xuất hành động:**
- Tỷ lệ chất lượng đạt khá cao (97,32%), nhưng vẫn còn tồn tại một số lỗi lặp lại, đặc biệt là **cao linh kiện C12** chiếm tỷ lệ lớn nhất.  
- Cần kiểm tra lại thiết lập máy móc và chương trình AOI để đảm bảo độ chính xác trong nhận diện vị trí và chiều cao linh kiện.  
- Đánh giá lại quy trình nạp linh kiện (pick &amp; place) cho các loại linh kiện C12, BGA và đầu cắm – có thể do lỗi định vị hoặc lực ép.  
- Đề xuất kiểm tra lại nguồn nguyên liệu và điều kiện bảo quản của các linh kiện nhạy cảm như BGA.

---

✅ **Tình trạng tổng thể:** Đạt yêu cầu, nhưng cần theo dõi sát các lỗi chính để cải thiện liên tục.  

**Người lập báo cáo:** Chuyên gia Chất lượng AOI  
**Ngày báo cáo:** 14/07/2026</pre>

<sub>513 token sinh ra, 2136ms</sub>

</td>
<td valign="top">

<pre style="white-space:pre-wrap">**BÁO CÁO CHẤT LƯỢNG HÀNG NGÀY - TÓM TẮT**

**Thời gian báo cáo:** 13/07/2026 đến 14/07/2026  
**Tổng số lượng sản phẩm kiểm tra:** 3.540 pcs  
**Số lượng đạt tiêu chuẩn (OK):** 3.445 pcs  
**Số lượng không đạt (NG):** 63 pcs  
**Tỷ lệ xuất khẩu (Yield Rate):** 97,32%

---

### **Các lỗi phổ biến nhất:**
1. **Cao linh kiện C12:** 6 lỗi (9,52%)
2. **Điện trở shunt:** 2 lỗi (3,17%)
3. **Đồng phẳng BGA:** 2 lỗi (3,17%)
4. **Nghiêng linh kiện:** 2 lỗi (3,17%)
5. **Cao đầu cắm:** 2 lỗi (3,17%)

---

### **Nhận xét:**
- Tỷ lệ xuất khẩu cao (97,32%), cho thấy quy trình kiểm soát chất lượng đang hoạt động hiệu quả.
- Các lỗi chủ yếu tập trung ở các loại linh kiện liên quan đến độ cao và vị trí lắp đặt. Cần theo dõi kỹ các bước lắp ráp để giảm thiểu sai sót.

**Không có bất thường nào được ghi nhận trong khoảng thời gian này.**

---  
*Trích từ báo cáo chất lượng AOI ngày 13-14/07/2026*</pre>

<sub>369 token sinh ra, 2062ms</sub>

</td>
</tr>
</table>

> ### 🗳 Phán quyết cặp P3 — Tóm tắt chất lượng
>
> Nhắc lại: **Model 1 ở đây là cùng một model với Model 1 ở mọi cặp khác.**
>
> **Model nào viết tiếng Việt tốt hơn ở cặp này?**  ☐ Model 1  ☐ Model 2  ☐ ngang nhau
>
> **Mức chênh:**  ☐ rõ rệt  ☐ nhẹ
>
> **Vì sao (câu/đoạn cụ thể khiến bạn quyết như vậy):**
>
> ______________________________________________________________



---

## Prompt 4 — Trợ lý tri thức — hỏi đáp vận hành

**Nguồn:** `server/services/aiLocalKnowledgeService.ts` — 1136-1166 (hàm generateWithOllama()) + 844-909 (getSystemPromptForRole, copy verbatim) + 1593 (retrieveKnowledge(), gọi thật)

**Ghi chú:** Câu hỏi test: "Ngưỡng cảnh báo tỷ lệ NG được cấu hình như thế nào và ai được thông báo khi vượt ngưỡng?". retrieve.language="vi", retrieve.intent="general", 5 citation thật từ knowledge/chunks.jsonl (knowledge/features/monitoring/ng-rate-threshold.md, knowledge/features/monitoring/ng-rate-threshold.md, knowledge/features/alerts/predictive-alerts.md, knowledge/features/alerts/alerts-list.md, knowledge/domain/aoi-thresholds.md). Bỏ 2 khối rỗng hợp lệ (historyBlock, toolBlock) + 1 khối bị lược (hintsBlock, logic regex không export) — xem comment trong mã nguồn script.

### User prompt (dùng chung cho cả 2 model)

```
Trợ lý kỹ thuật SYNAPSE on-prem cho kỹ sư. Trả lời tiếng Việt, kèm API/schema/cấu hình/CLI cụ thể; giải thích thiết kế và xử lý lỗi. Cấu trúc: (1) Tóm tắt 1–2 câu, (2) Các bước đánh số nêu *làm gì + ở đâu trong UI + kết quả*, (3) Lưu ý/lỗi thường gặp, (4) Liên quan 2 chủ đề. 200–450 từ. KHI ngữ cảnh có API/biến/lệnh, BẮT BUỘC trích lại trong backtick hoặc code-fence ```bash/```sql/```ts. Chỉ dùng tài liệu được cấp; không bịa API/endpoint/biến/bảng. Không nhắc Alibaba/AWS/GCP/Azure. Thiếu dữ kiện thì nói rõ chưa có.
NGUYÊN TẮC TRẢ LỜI:
1. Chỉ trả lời dựa trên ngữ cảnh được cung cấp; trích dẫn nguồn bằng [1], [2].
2. Nếu ngữ cảnh KHÔNG liên quan trực tiếp đến câu hỏi, hãy trả lời chính xác: "Tôi không có thông tin chính xác về câu hỏi này trong tài liệu hiện tại." và đề xuất câu hỏi rõ hơn. KHÔNG bịa.
3. Trả lời đúng trọng tâm câu hỏi hiện tại; bỏ qua phần ngữ cảnh không liên quan.
4. Nếu có dữ liệu thời gian thực, ƯU TIÊN dùng nó; không bịa số liệu.
5. TUYỆT ĐỐI KHÔNG lặp lại, sao chép, hoặc tóm tắt các câu trả lời trước trong Lịch sử hội thoại. Lịch sử CHỈ dùng để hiểu ngữ cảnh (ví dụ: đại từ, chủ đề đang nói tới). CHỈ trả lời cho 'Câu hỏi hiện tại' bên dưới, không nhắc lại nội dung cũ.
Phân loại ý định: general
Ngôn ngữ: vi
=== Ngữ cảnh từ knowledge base ===
[1] Ngưỡng NG Rate (part 1) | knowledge/features/monitoring/ng-rate-threshold.md
# Ngưỡng NG Rate

## Mục đích
Cấu hình ngưỡng cảnh báo NG Rate (tỉ lệ lỗi) cho từng station/máy, để hệ thống tự phát alert khi vượt ngưỡng trong cửa sổ thời gian định trước.

## Vị trí truy cập
- Menu: `Monitoring` › `MQTT` › `NG Rate Threshold`
- URL: `/mqtt/ng-rate-threshold`
- Vai trò: admin, engineer, manager

## Quyền yêu cầu
- Resource: `mqtt_ng_rate_threshold`
- Actions: `view`, `create`, `update`, `delete`
- Middleware: `requirePermission('mqtt_ng_rate_threshold')`

## Tiền điều kiện
- Đã đăng ký máy/station trong hệ thống.
- Đã có dữ liệu inspection để tính NG Rate.

## Các bước thao tác
1. **Mở trang** — Bảng hiện rules: `station`, `threshold %`, `window`, `minSamples`, `enabled`.
2. **Thêm rule** — Nút `+ Add Rule`. Nhập:
   - `Station/Machine` (chọn từ dropdown).
   - `Threshold` (vd: 5.0%).
   - `Window` (vd: 30 minutes).
   - `Min samples` (vd: 50 — tránh nhiễu khi sample ít).
   - `Severity` (warning / critical).
3. **Save** — Tạo bản ghi `mqtt_ng_rate_thresholds`.
4. **Test rule** — Nút `Simulate`: nhập NG count + total → preview có trigger alert không.
5. **Bật/tắt** — Toggle `enabled` từng rule không cần xóa.

## Kết quả mong đợi
- Khi NG Rate vượt threshold trong window, hệ thống ghi vào `alerts` và publish topic `factory/{id}/alerts/ng-rate`.
- Email/SMS gửi nếu rule liên kết với notification channel.

## Lỗi thường gặp & cách xử lý
| Triệu chứng | Nguyên nhân | Cách xử lý |
|---|---|---|
| Alert spam liên tục | `minSamples` quá thấp | Tăng lên 100+ |
| Không có alert dù NG cao | `enabled=false` | Bật rule |
| Window quá ngắn | False positive | Tăng lên 15-30 phút |

## API liên quan
- `tRPC: ngRateThreshold.list / create / update / delete / simulate`.

[2] Ngưỡng NG Rate (part 2) | knowledge/features/monitoring/ng-rate-threshold.md
## Tính năng liên quan
- [Cảnh báo MQTT](../alerts/mqtt-alert-rules.md) — engine xử lý alert.
- [Cảnh báo](../alerts/alerts-list.md) — xem alerts đã trigger.
- [SPC Analysis](../analytics/spc-analysis.md) — phân tích sâu hơn.

## Ví dụ thực tế
Tình huống: "Line A station St-3 thường có NG ~2%, muốn alert khi vượt 4% trong 15 phút".
Bước: Add rule station `St-3-LineA`, threshold `4.0%`, window `15m`, minSamples `60`, severity `warning`. Simulate với NG=3, Total=60 → 5% > 4% → trigger. Save.

[3] Cảnh báo Dự báo (Predictive Alerts) (part 1) | knowledge/features/alerts/predictive-alerts.md
# Cảnh báo Dự báo (Predictive Alerts)

## Mục đích
Hiển thị và quản lý các cảnh báo do mô hình AI/ML dự báo trước khi sự cố thực sự xảy ra: dự báo máy sắp lỗi, NG rate sắp vượt ngưỡng, drift mô hình, MTBF giảm. Cho phép QC/Manager hành động phòng ngừa thay vì chỉ phản ứng.

## Vị trí truy cập
- Menu: `Menu chính` › `Cảnh báo` › `Cảnh báo dự báo`
- URL: `/predictive-alerts`
- Vai trò thấy menu: admin, supervisor

## Quyền yêu cầu
- Resource: `analytics`
- Actions: `analytics_predictive_alerts`
- Middleware: `protectedProcedure` + `requirePermission('analytics_predictive_alerts')`

## Tiền điều kiện
- Đã có dữ liệu lịch sử ≥ 14 ngày (inspection, machine telemetry) để mô hình train được.
- Service `predictiveAlertService` đang chạy (cron mỗi 15 phút mặc định).
- Có ít nhất 1 mô hình dự báo đã được publish (xem [AI Model Management](../ai/ai-model-management.md)).

[4] Danh sách Cảnh báo (Alerts List) (part 1) | knowledge/features/alerts/alerts-list.md
# Danh sách Cảnh báo (Alerts List)

## Mục đích
Trung tâm tổng hợp tất cả cảnh báo hệ thống: cảnh báo từ máy (offline, error), từ inspection (NG rate vượt ngưỡng), từ MQTT rules, từ AI predictive, từ SPC. Cho phép xem, filter, acknowledge, resolve, gán người xử lý, ghi nhận root-cause.

## Vị trí truy cập
- Menu: `Menu chính` › `Cảnh báo` › `Danh sách cảnh báo`
- URL: `/alerts`
- Vai trò thấy menu: admin, supervisor, quality_inspector

## Quyền yêu cầu
- Resource: `alerts`
- Actions: `alerts_view` (xem); `alerts_acknowledge` (ack/resolve)
- Middleware: `protectedProcedure` + `requirePermission('alerts_view')`

## Tiền điều kiện
- Hệ thống đã chạy ít nhất 1 trong các nguồn cảnh báo: MQTT alert rules, predictive alerts, SPC alerts, machine offline detector.
- Có user đang đăng nhập với quyền `alerts_view`.

[5] AOI Thresholds & Calibration (Ngưỡng và Hiệu Chỉnh AOI) (part 1) | knowledge/domain/aoi-thresholds.md
# AOI Thresholds & Calibration (Ngưỡng và Hiệu Chỉnh AOI)

## Ngưỡng NG Rate (Tỷ Lệ Lỗi)

### Phân cấp cảnh báo tiêu chuẩn

| Level | Ngưỡng NG Rate | Hành động |
|-------|---------------|-----------|
| Normal | < 0.5% | Sản xuất bình thường |
| Warning | 0.5% – 1.0% | Cảnh báo vàng, theo dõi tăng cường |
| Alert | 1.0% – 2.0% | Cảnh báo đỏ, báo cáo kỹ sư |
| Critical | > 2.0% | Dừng line, điều tra nguyên nhân gốc rễ |

**Lưu ý:** Ngưỡng có thể khác nhau theo loại sản phẩm và yêu cầu khách hàng (IPC-A-610 Class 2 vs Class 3).

---

## Ngưỡng Confidence Score (Độ Tin Cậy AOI)

Mỗi phán đoán của máy AOI có confidence score:

| Score | Ý nghĩa | Hành động |
|-------|---------|-----------|
| > 90% | Tin cậy cao | Tự động pass/fail |
| 70% – 90% | Cần xem xét | Chờ operator confirm |
| < 70% | Không chắc | Kỹ sư xem xét thủ công |

---

## Calibration (Hiệu Chỉnh Máy)

### Lịch bảo trì và hiệu chỉnh

| Loại | Tần suất | Người thực hiện |
|------|---------|----------------|
| Daily warm-up | Mỗi ca | Operator |
| Reference board check | Hàng ngày | Operator |
| Camera calibration | Hàng tuần | Technician |
| Full calibration | Hàng tháng | Kỹ sư AOI |
| Lighting check | Hàng tuần | Technician |

### Quy trình hiệu chỉnh reference board
1. Lấy reference board từ kho bảo quản.
2. Chạy reference board qua máy AOI theo chương trình chuẩn.
3. Kiểm tra kết quả so với golden standard:
   - Tất cả lỗi đã biết phải được phát hiện (0 miss).
   - False alarm ≤ 2 trên toàn bộ board.
4. Nếu không đạt → dừng sản xuất, gọi kỹ sư.
5. Ghi nhận kết quả vào log hiệu chỉnh.

### Lighting Calibration
- Kiểm tra độ đồng đều ánh sáng bằng white calibration board.
- Sai lệch cho phép: ±5% so với giá trị chuẩn.
- Nếu lệch > 5%: vệ sinh lens, kiểm tra LED ring.

---

=== Câu hỏi hiện tại ===
Ngưỡng cảnh báo tỷ lệ NG được cấu hình như thế nào và ai được thông báo khi vượt ngưỡng?
=== Câu trả lời (chỉ trả lời câu hỏi hiện tại, không lặp lại lịch sử) ===
```

### Cặp câu trả lời

<table>
<tr><th width="50%">Model 1</th><th width="50%">Model 2</th></tr>
<tr>
<td valign="top">

<pre style="white-space:pre-wrap">(1) Ngưỡng cảnh báo tỷ lệ NG được cấu hình thông qua trang `Monitoring` › `MQTT` › `NG Rate Threshold`, nơi kỹ sư có thể thiết lập ngưỡng phần trăm, cửa sổ thời gian, số mẫu tối thiểu và mức độ nghiêm trọng cho từng máy/station. Khi vượt ngưỡng, hệ thống sẽ phát alert và gửi thông báo đến người được chỉ định qua email/SMS nếu đã liên kết kênh thông báo.

(2) Các bước thực hiện:
1. **Mở trang cấu hình** — Truy cập `/mqtt/ng-rate-threshold` trong UI; hiển thị bảng `station`, `threshold %`, `window`, `minSamples`, `enabled`.  
2. **Thêm rule mới** — Nhấn nút `+ Add Rule`; chọn `Station/Machine` từ dropdown (ví dụ: `St-3-LineA`).  
3. **Nhập thông số** — Thiết lập:
   - `Threshold`: 4.0%,
   - `Window`: 15m,
   - `Min samples`: 60,
   - `Severity`: warning.
4. **Lưu cấu hình** — Nhấn Save → tạo bản ghi trong bảng `mqtt_ng_rate_thresholds`.  
5. **Kiểm thử rule** — Dùng nút `Simulate`, nhập NG count = 3, Total = 60 → tỷ lệ = 5% &gt; ngưỡng → trigger alert.  
6. **Bật rule** — Đảm bảo `enabled` được bật để kích hoạt cảnh báo.

(3) Lưu ý/lỗi thường gặp:
- Nếu không có alert dù NG cao: kiểm tra `enabled=false` → bật lại [1].
- Alert spam liên tục: `minSamples` quá thấp (dưới 50) → tăng lên ≥100 [1].
- False positive do window quá ngắn (ví dụ &lt;10 phút): điều chỉnh lên 15–30 phút [1].

(4) Liên quan đến:
- **Cảnh báo MQTT** — Hệ thống sử dụng engine cảnh báo MQTT để xử lý trigger từ rule NG Rate [2].  
- **Danh sách Cảnh báo (Alerts List)** — Tất cả alert từ ngưỡng NG Rate được ghi vào `alerts` và hiển thị tại `/alerts`, cho phép kỹ sư xác nhận, giải quyết và ghi root cause [4].

&gt; API liên quan: `tRPC: ngRateThreshold.create(...)` để thêm rule; `ngRateThreshold.simulate()` để kiểm thử logic.</pre>

<sub>539 token sinh ra, 2645ms</sub>

</td>
<td valign="top">

<pre style="white-space:pre-wrap">(1) **Tóm tắt**: Ngưỡng cảnh báo tỷ lệ lỗi NG được cấu hình trong giao diện `Monitoring` › `MQTT` › `NG Rate Threshold`. Khi ngưỡng bị vượt trong khoảng thời gian quy định, hệ thống sẽ tạo alert và thông báo qua email/SMS nếu có kênh liên kết.

(2) **Các bước cấu hình**:
1. **Truy cập UI**: Vào menu `Monitoring` › `MQTT` › `NG Rate Threshold` (`/mqtt/ng-rate-threshold`) [1].
2. **Tạo rule mới**: Nhấn nút `+ Add Rule`, chọn `Station/Machine`, nhập `Threshold %`, `Window`, `Min samples`, và `Severity`.
3. **Lưu rule**: Bấm Save để tạo bản ghi trong bảng `mqtt_ng_rate_thresholds`.
4. **Kiểm tra rule**: Dùng nút `Simulate` để thử nghiệm với số liệu NG/Total → xác định có trigger alert không.
5. **Kích hoạt rule**: Bật toggle `enabled` nếu muốn cảnh báo được kích hoạt.

(3) **Lưu ý và lỗi thường gặp**:
- Nếu `minSamples` quá thấp (dưới 50), có thể gây spam alert do nhiễu dữ liệu.
- Nếu `enabled=false`, rule sẽ không phát sinh cảnh báo dù vượt ngưỡng.
- Khoảng thời gian `window` ngắn (&lt;15 phút) dễ dẫn đến false positive.

(4) **Liên quan hai chủ đề**:
- [Cảnh báo MQTT](../alerts/mqtt-alert-rules.md): quy trình xử lý alert từ rule NG Rate Threshold.
- [Danh sách Cảnh báo (Alerts List)](../alerts/alerts-list.md): nơi xem, quản lý và phản hồi cảnh báo đã phát sinh.

API liên quan:
```ts
tRPC: ngRateThreshold.list / create / update / delete / simulate
```

Cấu hình lưu trong bảng `mqtt_ng_rate_thresholds`, alert được publish lên topic:
```bash
factory/{id}/alerts/ng-rate
```</pre>

<sub>445 token sinh ra, 2878ms</sub>

</td>
</tr>
</table>

> ### 🗳 Phán quyết cặp P4 — Trợ lý tri thức
>
> Nhắc lại: **Model 1 ở đây là cùng một model với Model 1 ở mọi cặp khác.** Sau cặp này, điền **PHÁN QUYẾT TỔNG HỢP** ở phiếu đầu trang.
>
> **Model nào viết tiếng Việt tốt hơn ở cặp này?**  ☐ Model 1  ☐ Model 2  ☐ ngang nhau
>
> **Mức chênh:**  ☐ rõ rệt  ☐ nhẹ
>
> **Vì sao (câu/đoạn cụ thể khiến bạn quyết như vậy):**
>
> ______________________________________________________________



---

## Vệ sinh VRAM (xác nhận từng lượt)

| Mốc | VRAM (MiB used/total) |
|---|---|
| Trước khi bắt đầu | {"usedMib":1006,"totalMib":32607} |
| Sau dọn model embed (trước Phase 1) | {"usedMib":1487,"totalMib":32607} |
| Sau dispose model đầu tiên nạp (Phase 1) | {"usedMib":1493,"totalMib":32607} |
| Sau dispose model thứ hai nạp (Phase 2, cuối cùng) | {"usedMib":1496,"totalMib":32607} |

Lệnh đo (chạy độc lập sau khi script kết thúc):
```bash
nvidia-smi --query-gpu=memory.used,memory.total --format=csv,noheader,nounits
tasklist | grep node.exe   # (PowerShell: Get-Process node -ErrorAction SilentlyContinue)
```

---

# ✅ PHÁN QUYẾT CỦA CHỦ DỰ ÁN (2026-08-02) — ĐÃ CHẤM

**Nguyên văn:** *"Model 2 trả lời chuẩn kỹ thuật hơn và dễ hiểu hơn nhưng ở mức độ nhẹ. Cơ bản 2 model đều trả lời tiếng Việt ổn, chỉ là văn phong của Model 2 dễ hơn với dẫn kỹ thuật và người mới."*

## Bảng ánh xạ — nay công bố

| Nhãn | Model thật |
|---|---|
| **Model 1** | `Qwen3-30B-A3B-Instruct-2507-UD-Q4_K_XL.gguf` — **General** |
| **Model 2** | `Qwen3-Coder-30B-A3B-Instruct-UD-Q4_K_XL.gguf` — **Coder** |

⚠ **Cách khôi phục ánh xạ** (file gốc đã bị controller xoá cùng workspace sau khi push — lỗi quy trình, ghi lại để không lặp): chạy lại `scripts/ai-survey/vi-quality-ab.mjs`, đối chiếu đầu ra theo cặp. Hợp lệ vì **temperature=0 là giải mã tham lam** ⇒ cùng model + cùng prompt cho ra **cùng đầu ra từng ký tự**. Khối P1 của lượt chạy lại khớp **byte-for-byte** với "Model 1" trong file này.

## Nghĩa là gì

**Model chuyên code viết tiếng Việt NHỈNH HƠN model general** — ngược với lo ngại nêu trong spec chiến lược (*"model chuyên code viết văn xuôi tiếng Việt thường khô và hay lẫn thuật ngữ Anh"*). **Dự đoán đó SAI.**

Nhưng đọc đúng mức: chủ dự án nói **"mức độ nhẹ"** và **"cả hai đều ổn"**. ⇒ Kết luận đúng là **"chất lượng tiếng Việt KHÔNG còn là lý do để giữ model general riêng"**, chứ không phải "Coder vượt trội".

## Hệ quả cho quyết định roster

1. **Nhánh điều kiện của spec chiến lược §7.5 nay đã chốt được**: *"nếu §4 cho thấy tiếng Việt của Coder-30B chấp nhận được → roster A"* ⇒ **điều kiện ĐẠT**.
2. **Hồ sơ `balanced` mất lý do tồn tại chính.** Nó sinh ra làm **phương án dự phòng** phòng khi Coder viết tiếng Việt tệ. Rủi ro đó **không xảy ra** ⇒ `balanced` chỉ còn giá trị nếu cần model general vì lý do **khác** (chưa có lý do nào được đo).
3. **Nút thắt còn lại KHÔNG phải chọn model** — mà là **ngân sách thị giác** và **bí ẩn CUDA context**. Xem spec chiến lược §5 bước A/B/C.
