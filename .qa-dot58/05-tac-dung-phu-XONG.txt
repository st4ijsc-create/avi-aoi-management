ĐỢT 58 — KHAI ĐỦ TÁC DỤNG PHỤ GHI VÀO CÂY (không xoá gì, không khôi phục gì)

A. Thư mục của CHÍNH tôi: .qa-dot58/** (616 tệp đã commit; src-nen/, dist-*/, state-*.json bị .gitignore
   loại — src-nen chứa bản sao .env, state-*.json chứa cookie phiên test).

B. Thư mục KHÔNG tracked của ĐỢT CŨ mà harness mượn có ghim đường ra — tôi chỉ THÊM, không xoá:
   · .qa-dot47/e2e-lai-20260913-073148/   ← e2e spec Đợt 31 tự ĐỔI đường ra để KHÔNG đè bằng chứng cũ
                                            (đúng cơ chế chống ghi đè của chính spec; 103 tệp 0 byte
                                            của .qa-dot47 vẫn nguyên: đếm lại 103)
   · .qa-dot44/58a/                        ← f1-fonts.mjs ghim gốc .qa-dot44 cho --tag
   · .qa-dot44/mang-ngoai/{12292,15660,25888}.json ← nhật ký "request ra ngoài" của mang.mjs
   · .qa-dot44/chan/E/e9-*.json            ← do35.mjs (ca e9) ghim đường ra; hàng msl TẠM đã chèn→xoá,
                                            msl 7 814 trước = 7 814 sau

C. .qa-pw-output/  ← outputDir cấu hình sẵn trong playwright.config.ts (G140). test-results/ KHÔNG đụng:
   5 ảnh commit md5 5/5 khớp, git status rỗng.

D. KHÔNG chạy `kb:operational-cards:test` ⇒ 169 tệp tracked dưới knowledge/ trong git status là của
   PHIÊN KHÁC; tôi không sửa, không khôi phục (đúng G138).
