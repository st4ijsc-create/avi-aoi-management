LÔ D + E — XONG
================
Thời điểm: bắt đầu 2026-09-15 08:55 (+07), xong 2026-09-15 09:25 (+07).
Máy đo: cổng 3064 (PID 19136, bản dựng .qa-tapdoan/dist-a147fc35, bundle index-DxxtO34H.js).
KHÔNG bật/tắt server nào. KHÔNG chạm 3000/3001/3008. Nhịp heartbeat nền để nguyên (log nhip.log chạy suốt).
Không sửa một tệp nào ngoài .qa-tapdoan/. Không git. Không chạm .env.

KẾT QUẢ: 26 ô — ĐẠT 17 · SAI 6 · CHẶN-ĐÚNG 3 · HỎNG 0 · N/A 0 · ô không phán quyết 0.
Bảng đầy đủ: .qa-tapdoan/BANG-DE.md
Thô:         .qa-tapdoan/tho/DE/*.json
Ảnh:         .qa-tapdoan/anh/DE-*.png (38 tệp)

SÁU Ô SAI (tóm tắt):
  #6  D1  "ghi chú" khai trong nganXuLyLogic.ts nhưng 0 nút ở cả 5 vai (kể cả admin)
  #7  D1  /twin/may/4568 (nhà máy THỨ HAI của kythuat) = ngoaiPhamVi — TwinMay.tsx:285 factories[0]
  #8  D2  vì #7, không tạo được phiếu qua UI cho máy 4568 như brief đòi
  #12 D4  nhóm "Mở chức năng" của quanly KHÔNG có /corporate-dashboard và /oee-dashboard
          (loaiDich không bao giờ được truyền ⇒ 2 dòng bảng §9.3 có mã, 0 chỗ gọi)
  #22 E3  Studio chỉ thiết kế được TẦNG 1 của TOÀ 1 — TwinStudio.tsx:106 và :125 lấy [0], 0 ô chọn toà/tầng
  #25 E6  quanly (0 canCreate) vẫn thấy nút "Generate" và "Create building" bật sẵn

LỆNH CHẠY LẠI (thứ tự đã dùng; mọi ca có GHI đều đi qua do-DE.sh vì nó mang trap EXIT INT TERM):
  # nền / không ghi
  node .qa-tapdoan/db-DE.mjs dem
  node .qa-tapdoan/db-DE.mjs quyen
  node .qa-tapdoan/do-DE.mjs E7                      # API, chặn-đúng (KHÔNG gọi bằng admin)
  node .qa-tapdoan/do-DE.mjs E1                      # 5 vai
  node .qa-tapdoan/do-DE.mjs E2E3                    # kythuat + quanly
  node .qa-tapdoan/do-DE.mjs D1                      # 5 vai
  node .qa-tapdoan/do-DE.mjs D4                      # quanly + congnhan
  node .qa-tapdoan/do-DE.mjs D5
  node .qa-tapdoan/do-DE.mjs E4
  node .qa-tapdoan/do-DE.mjs E6
  node .qa-tapdoan/tho/DE/probe-nut-tat.mjs          # phụ lục: nút tắt có nhìn ra được không
  # có GHI THẬT — luôn qua wrapper có trap
  sh .qa-tapdoan/do-DE.sh D3                         # ablation 3 mốc: truoc → chèn → co(+ack) → xoá → sau
  sh .qa-tapdoan/do-DE.sh D2                         # phiếu bảo trì: UI 4568 (chặn) · UI 4197 (dương) · API 4568
  sh .qa-tapdoan/do-DE.sh E5                         # vùng an toàn: vẽ → lưu → xoá qua UI
  sh .qa-tapdoan/do-DE.sh don                        # DỌN (chạy được bất cứ lúc nào, idempotent)

DỌN: đã chạy lúc 09:21:53. andon 62 (raised 55) · maintenance_work_orders 29 · twin_vat_the 341 ·
twin_toa_nha 13 · twin_tang 85 · twin_dat_cho 2420 — TRÙNG KHÍT số nền lúc 08:50.
Còn lại 2 hàng audit_logs (id 5765, 5778, entityName 'QATD-DE-RAISED-M4977') vì vai DB của ứng dụng
KHÔNG có quyền DELETE trên audit_logs (append-only). Xem mục HÀNG TẠM trong BANG-DE.md.

ĐIỀU TÔI KHÔNG CHẮC
-------------------
1. Ô #2 (quanly · ẩn tạm). Tôi chấm ĐẠT vì `machine_control.canCreate = false` trong DB, nhưng bảng §2
   của BRIEF ghi "ẩn tạm: có". Không có cách đọc nào làm nó thành lỗi sản phẩm — song NẾU chủ đợt định
   cấp canCreate cho quanly thì bộ sinh tài khoản phải sửa và ô này phải ĐO LẠI. Tôi không sửa bộ sinh.
2. Ô #12 (D4). Tôi chỉ đo nhóm "Mở chức năng" của NGĂN XỬ LÝ ở cấp MÁY. Nếu `/corporate-dashboard` và
   `/oee-dashboard` được coi là phải tới từ chỗ khác trên `/twin` (breadcrumb, thẻ KPI, dải Line), thì
   phép đo của tôi không phủ chỗ đó — nhưng ngăn xử lý thì chắc chắn không có.
3. Ô #22 (E3). Tôi CHƯA thử đường "đổi nhà máy trong Studio rồi xem tầng đầu của nhà máy đó" cho công ty
   B: kết luận "chỉ tầng 1 toà 1" suy từ (a) mã `[0]` ở hai chỗ, (b) 0 bộ chọn toà/tầng trên màn, (c) con
   số 45 placed / 326 unplaced khớp đúng số máy của tầng 81. Nếu có một đường vào tầng khác mà tôi không
   thấy (ví dụ qua URL query mà mã không đọc), kết luận phải xét lại — tôi grep `setToaNha|setTang|
   chon-toa|chon-tang` trong TwinStudio.tsx ra 0 kết quả, nhưng đó là grep một tệp.
4. Ô #25 (E6). Tôi KHÔNG bấm "Create building"/"Generate" bằng vai quanly (bấm là ghi thật / ghi đè bố
   cục). Kết luận "bấm sẽ ăn 403" suy từ cổng server đọc được (`dungNhaXuong` đứng trên
   `quyenThietKe("canCreate")` = settings_factory ∥ machine_control canCreate; `sinhTuDong` là
   `adminProcedure`) chứ KHÔNG phải từ một lượt gọi thật. Đợt sau muốn chốt thì gọi API bằng cookie
   quanly với payload hợp lệ và kỳ vọng 403 — nhưng phải chấp nhận rủi ro nếu cổng hở.
5. Giao diện hiển thị **tiếng Anh** ở mọi lượt đo (trình duyệt sạch, mặc định en-US). Tôi đọc theo NGHĨA
   trên chữ tiếng Anh. Nếu có khác biệt nhãn giữa vi/en trong ngăn xử lý thì lô F (F6 i18n) mới thấy.
6. Lượt đo D1 ĐẦU TIÊN của kythuat đọc ra "0 nút" vì màn còn ĐANG TẢI (cửa sổ chờ cố định 7 s không đủ
   trên 1.108 máy). Tôi đã đổi sang chờ tới khi `ngan-ma-may` hoặc `may-khong-mo-duoc` xuất hiện rồi
   ĐO LẠI CẢ 5 VAI trên cùng thiết bị. Số trong bảng là của lượt thứ hai. Không có ô nào giữ số lượt 1.
