# BẢNG V2 — ĐO LẠI VÒNG 2 SAU BA BẢN VÁ (QA lần 11, 3D twin, kịch bản tập đoàn)

**Bản dựng đo:** `.qa-tapdoan/dist-sauva`, bundle `assets/index-65hzR42G.js`, server cổng **3064** PID **13348** (không bật/tắt, không chạm 3000/8080).
**Dữ liệu:** sinh lại, cầu chì 32/32 — QATD-A **41** (line đầu 315, máy đầu 5305, 371 máy) · QATD-B **42** (line 347, máy 5676, 409) · QATD-C **43** (line 382, máy 6085, 328). Nhịp heartbeat 45 s chạy nền suốt lượt đo.
**Thiết bị đo:** Playwright chromium, `--use-angle=default --enable-gpu --ignore-gpu-blocklist` (PH-25), **1 worker** (mọi ca chạy tuần tự, mỗi ca một `browserContext` riêng), viewport 1600×900 trừ H3 (1280×720).
**Kịch bản:** `.qa-tapdoan/do-vong2.mjs` · thô `.qa-tapdoan/tho/V2/*.json` · ảnh `.qa-tapdoan/anh/V2-*.png` · kỳ vọng DB `.qa-tapdoan/tho/V2/db-V2.json` + `db-V2c.json` (CHỈ SELECT).
**Giao diện các tài khoản QATD là TIẾNG ANH** — mọi câu "nguyên văn" dưới đây là chữ người dùng thật sự đọc.

---

## NHÓM 1 — ca vòng 1 SAI, kỳ vọng nay ĐẠT

| ca | vai | kỳ vọng (từ DB, đo rời) | số đo vòng 2 | phán quyết | vì sao |
|---|---|---|---|---|---|
| **R1** mở chuyền đầu QATD-B (line **347**) qua **cây phân cấp** | `qatd_kythuat` | `Line 1`, **15 máy · 15 trạm**, cảnh vẽ **15 khối** | URL `/twin/line/347` · `ten-line` = **"Line 1"** · header **"Machines 15 · Stations 15 · WIP 65"** · **khối máy trong cảnh = 15** (`__demTuongTac.dsMay`) · đối chứng độc lập `__demNhan.tong` = **15** ngay ở lượt tới-bằng-cây · canvas 1, `__soCanvas` = 1 · `line-rong` 0, `line-khong-mo-duoc` 0 | **ĐẠT** | vòng 1: *"Máy 0 · Trạm 0"*, 0 khối, `line-rong`. PH-12 đóng: trang không còn hỏi `factories[0]`. Ảnh `V2-R1-qatd_kythuat-line347.png` |
| **R2** mở chuyền QATD-C ở **toà 3** (line **397**, `QATD-C-T3-X1-L1`) | `qatd_congnhan` | `Line 1`, **10 máy · 10 trạm**, cảnh vẽ **10 khối** | header **"Machines 10 · Stations 10 · WIP 56"** · **khối = 10** · `__demNhan.tong` = **10** · 10 nhãn M01–M10 trên cảnh · dải trạm 2D S01–S10 (prefix `QATD-C-T3-X1-L1-`) | **ĐẠT** | vòng 1 ô C1.4: header đúng nhưng **0 khối** — cảnh rỗng. Đây là ca bằng chứng thị giác mạnh nhất; ảnh `V2-R2-qatd_congnhan-line397.png` |
| **R3** `/twin/may/5676` (máy của QATD-B) | `qatd_kythuat` | mở được; `WAVE_SOLDER-01`, loại `WAVE_SOLDER`; `__soCanvas` = 1 | `man-twin-may` dựng · `ten-may` = **"WAVE_SOLDER-01"** · `loai-may` chứa **WAVE_SOLDER** · `khoi-canh-may` 1 canvas, `__soCanvas` = **1** · **KHÔNG** `may-khong-mo-duoc`, **KHÔNG** `may-chua-dat-cho` · `suc-khoe-may` hạng có giá trị | **ĐẠT** | vòng 1 ô C2.1: bị từ chối *"không thuộc phạm vi đang xem"*. Ảnh `V2-R3-qatd_kythuat-may5676.png` |
| **R4** máy QATD-C ở **toà 3** (`6247` SPI-01, DB có **1** hàng `twin_dat_cho` tầng 235 — đã SELECT xác nhận trước) | `qatd_congnhan` | KHÔNG còn *"chưa có chỗ trên bố cục 3D"* | `ten-may` = **"SPI-01"**, loại **SPI** · `may-chua-dat-cho` = **0** · canvas 1, `__soCanvas` = 1 | **ĐẠT** | vòng 1 ô C2.4: `may-chua-dat-cho` trong khi DB có hàng đặt chỗ |
| **R5** từ màn Máy QATD-B bấm **về màn chuyền** | `qatd_kythuat` | có nút, về đúng chuyền 347 **và nhà máy 42** | `ve-man-line` href = **`/twin/line/347`**, chữ **"Line 1"** · sau khi bấm: URL `/twin/line/347`, `ten-line` "Line 1", **"Machines 15"**, **khối = 15**, dải trạm prefix **`QATD-B-T1-X1-L1-`** ⇒ cảnh của **QATD-B**, không nhảy về QATD-A | **ĐẠT** | vòng 1 ô C5.1: không có đường về đúng chuyền. Ảnh `V2-R5-qatd_kythuat-sau-ve-line.png` |
| **R6** chuyền **ngoài phạm vi** phải nói câu KHÁC câu "chưa xếp chỗ" (PH-13) | `qatd_congnhan` → line 347 | hai câu KHÁC nhau | nhánh **`line-khong-mo-duoc`**, `data-ly-do` = **`ngoaiPhamVi`**, 0 canvas, ba ô đếm in **"—"**. **Nguyên văn A:** *"Cannot open line #347 — This item is not in the scope you are viewing (or no longer exists). Switch factory/floor and open it again."* + nút "Factory" + toast *"Could not find line."*<br>**Nguyên văn B** (nhánh `line-rong`, "chưa xếp chỗ"): *"This line has no machines on the layout yet — The line may not have been placed in Twin Studio yet, or it belongs to another factory."* | **ĐẠT** | vòng 1 ô C4a: **cùng một câu** cho hai thế giới. Nay hai câu khác hẳn, và câu A không xác nhận chuyền có thật (G82). ⚠ **Câu B lấy từ catalogue i18n trên đĩa + phép đo SỐNG của vòng 1** — xem "ĐIỀU TÔI KHÔNG CHẮC" |
| **R7** Studio: có `chon-toa-nha` + `chon-tang`; chọn **toà 2** rồi **tầng 2** | `qatd_kythuat` | ba ô chọn có thật; dải sức khoẻ đếm theo ĐÚNG tầng | `chon-nha-may` 2 mục · **`chon-toa-nha` 4 mục** (Toà 1–4) · **`chon-tang` 7 mục** (Tầng 1–7) · chọn toà 2 (id 66) ⇒ **68 khối** + dải *"68 machines placed · 303 awaiting placement"* (DB tầng 172 = 68; 371 − 68 = 303) · rồi chọn tầng 2 (id 173) ⇒ **45 khối** + *"45 machines placed · 326 awaiting placement"* (DB tầng 173 = 45; 371 − 45 = 326) · `__soCanvas` = **1** sau đổi tầng | **ĐẠT** | vòng 1 ô E3: `grep setToaNha\|setTang\|chon-toa-nha\|chon-tang` = 0 ⇒ chỉ tầng 1 toà 1, 326/371 máy không xếp chỗ được. ★ Bước GIỮA (toà 2/tầng 1 = **68**) là con số chỉ tầng 172 mới có ⇒ phân biệt được "đã đổi tầng" với "chưa đổi gì" (45 của tầng 173 TRÙNG 45 của tầng mặc định). Ảnh `V2-R7-kythuat-studio-toa2-tang2.png` |
| **R8** Studio vai có `canEdit`, **0 `canCreate`**, không admin | `qatd_quanly` (đo quyền qua API: `machine_control` V1 **C0** E1 **D0**, 0 hàng `settings_factory`) | KHÔNG thấy "Generate"/`nut-mo-sinh`, KHÔNG thấy đường tạo toà nhà — **ẩn hẳn**, không disable | `nut-mo-sinh` **0 phần tử trong DOM** · `tab-con-duong-b` (Add building) **0 phần tử** · `nut-tao` 0 · chữ "Generate" **không xuất hiện** trên toàn màn · cùng lúc `khoi-anh-nen` 0, `nut-tai-model` 0, `nut-luu-ban-ghi` 0 (đều `canCreate`), `nut-go-khoi-mat-bang` 0 (`canDelete`) · **đối chứng dương**: `nut-luu` **1** (disabled vì *"chưa có gì để lưu"*, đúng luật), `nut-che-do-translate` 1 ⇒ không vá quá tay | **ĐẠT** | vòng 1 ô E6: "Generate" render + enabled, và đi hết 3 bước tới "Create building" enabled rồi mới nhận 403. Ảnh `V2-R8-quanly-studio.png` (thanh tab chỉ còn "Choose drawing file \| Design") |

## NHÓM 2 — đối chứng âm, kỳ vọng GIỮ NGUYÊN

| ca | vai | kỳ vọng giữ | số đo vòng 2 | phán quyết | vì sao |
|---|---|---|---|---|---|
| **N-1** `/twin/may/5676` (máy QATD-B, ngoài phạm vi) | `qatd_congnhan` | vẫn bị từ chối | `may-khong-mo-duoc` = 1, `data-ly-do` = **`ngoaiPhamVi`**, **0 canvas**, **0 lộ tên/loại máy**. Nguyên văn: *"Cannot open machine #5676 — This item is not in the scope you are viewing (or no longer exists)…"* | **CHẶN-ĐÚNG** | bản vá PH-12 mở đúng cho người có phạm vi mà KHÔNG nới cho người ngoài. Ảnh `V2-N-1-qatd_congnhan-may5676.png` |
| **N-2** 3 lời gọi API bằng cookie `qatd_congnhan` | `qatd_congnhan` | NOT_FOUND / [] / null, **0 rò** | `factoryCommand.machineDetail(5676)` → **404 NOT_FOUND**, data `null` · `twinCanh.danhSachToaNha({factoryId:42})` → 200 **`[]`** (31 byte) · `twinCanh.noiCuaThucThe({loai:"may",id:5676})` → **404 NOT_FOUND** · thêm `{loai:"line",id:347}` → **404 NOT_FOUND** · **0** chuỗi `QATD-B` / `WAVE_SOLDER-01` / `QATD-B-T1` trong cả 5 phản hồi · **đối chứng dương cùng phiên**: `noiCuaThucThe({loai:"may",id:6247})` → **200** `{factoryId:43,toaNhaId:75,tangId:235}` và `danhSachToaNha({factoryId:43})` trả 4 toà | **CHẶN-ĐÚNG** | thủ tục MỚI `noiCuaThucThe` có hàng rào đặt TRƯỚC mọi lượt đọc và trả `NOT_FOUND` (không `FORBIDDEN`) ⇒ không xác nhận thực thể có thật |
| **N-3** `twinCanh.sinhTuDong` | `qatd_kythuat` (KHÔNG gọi bằng admin) | vẫn **403** | POST `sinhTuDong({factoryId:41,tangIds:[167]})` (tầng RỖNG 0 đặt chỗ, chọn cho an toàn) → **HTTP 403 FORBIDDEN** · **đối chứng dương cùng phiên**: `xemTruocSinh` (canView) → **200** | **CHẶN-ĐÚNG** | cổng `adminProcedure` không bị bản vá PH-15 làm lỏng; và UI ẩn nút không thay cổng server |
| **N-4** vào `/twin` | `qatd_khongquyen` | vẫn bị chặn | không dựng `man-twin-van-hanh`, **0 canvas**, chữ trên màn: *"Access denied — You don't have permission to access this page…"* + *"You don't have permission to: view."* | **CHẶN-ĐÚNG** | RouteGuard giữ nguyên |
| **N-5** phạm vi 7 vai qua `factoryCommand.overview` | 7 vai | giamdoc 1108 · quanly 371 · kythuat 780 · congnhan 328 · admin 1150 · khonggan 0 · khongquyen FORBIDDEN | **7/7 khớp**: 1108 (3 nhà máy) · 371 (1) · 780 (2) · 328 (1) · 1150 (5) · **0** (0 nhà máy, 103 byte) · **FORBIDDEN** | **ĐẠT** | ba bản vá không dịch chuyển một mẫu số nào |

## NHÓM 3 — hazard MỚI do chính bản vá sinh ra

| ca | kỳ vọng | số đo vòng 2 | phán quyết | vì sao |
|---|---|---|---|---|
| **H1** Studio: đổi tầng khi còn **thay đổi CHƯA LƯU** | phải hỏi, hoặc giữ lại | chọn máy `5333` trên cảnh → bật công tắc **Lock** ⇒ huy hiệu **"1 unsaved changes"** + nút Save bật. Đổi tầng (165 → 166) ⇒ **"1 unsaved changes" biến mất (0)**, **0 hộp thoại**, **0 toast**. Quay LẠI tầng 165 ⇒ vẫn **0** ⇒ thay đổi **mất hẳn, im lặng** | **SAI** | đúng thứ tác giả bản vá tự khai là nợ mới (`TwinStudio.tsx:371-377`): `key={tangDangChon.tangId}` ép remount, `beforeunload` chỉ chặn lượt rời TRANG. Nay đã **đo sống**, không còn là suy luận. Ảnh `V2-H1-truoc-doi-tang-co-thay-doi-chua-luu.png` → `V2-H1-sau-doi-tang.png` |
| **H2** `__soCanvas` **sau khi đổi tầng** ở Studio | phải về **1** | sau lượt đổi toà 1→2 rồi tầng 1→2: `__soCanvas` = **1**, `document.querySelectorAll("canvas")` = **1** (đo trong R7, thô `R7.json` khoá `H2`) | **ĐẠT** | RB-4 giữ được: remount không để lại context WebGL thứ hai. Agent vá chỉ *suy luận* 1→0→1; nay đo live |
| **H3** chiều cao `header` `/twin-studio` @1280×720 | docblock `BoChonNapUI` đòi giữ **48 px** | header Studio = **28,00 px**; `bo-chon-nap` = **215×24 px** (MỘT hàng, 3 ô chọn, không xuống dòng); canvas 541×251; **0 tràn ngang** · **đối chứng**: header `/twin` @1280 = **48,00 px** đúng bằng bất biến cũ, `bo-chon-nap` cũng 215×24, 0 tràn ngang | **ĐẠT** | ⚠ con số 48 px là bất biến của header màn **/twin** (chỗ gọi thứ nhất của kit), không phải của Studio — bố cục khác, tiêu đề `text-lg` mới là thứ quyết chiều cao 28 px. Hazard thật (3 ô thay 1 ô ⇒ xuống dòng / ăn vào canvas) **không xảy ra ở cả hai màn**. Ảnh `V2-H3-studio-1280x720.png`, `V2-H3-twin-1280x720-doi-chung.png` |
| **H4** thời gian mở `/twin/line/:id` và `/twin/may/:id` (thêm 1 lượt `noiCuaThucThe`) — 3 lượt/màn, **context mới mỗi lượt**, 1 worker | so mốc vòng 1: p50 **2.615 ms**, 48/48 > 2.500; admin **1.182 ms** | `kythuat` line 347: **2.193 / 2.085 / 2.074** ⇒ p50 **2.085**, max 2.193 · `kythuat` máy 5676: **2.137 / 2.160 / 2.162** ⇒ p50 **2.160**, max 2.162 · **p50 chung kythuat 2.137 ms, max 2.193 ms — 6/6 lượt DƯỚI 2.500** · `admin` line 347: **1.087 / 1.103 / 1.055** ⇒ p50 **1.087** | **ĐẠT** | −478 ms so p50 vòng 1 dù màn gọi THÊM một thủ tục. Mốc admin 1.087 ≈ 1.182 vòng 1 ⇒ sàn khởi động không hồi quy; khoản giảm đến từ bản vá phạm vi `commandCenter.hierarchy` (V-01) |
| **H5** Studio sau **auto-fit** (PH-29): khung 3D còn "gần trống"? | không còn gần trống | mở Studio (không thao tác gì): **45 khối, 45/45 trong khung**, nhưng bbox tâm khối = **182×81 px** trên canvas **726×431** ⇒ **4,74 %** diện tích. **ABLATION:** chỉ **BẤM MỘT KHỐI MÁY** (không bấm Fit) ⇒ nhảy lên **337×183 px = 19,72 %**; bấm tiếp **nút Fit** ⇒ **y hệt 337×183 = 19,72 %** (không đổi thêm) | **SAI** | auto-fit PH-29 **KHÔNG chạy ở lượt mount đầu**: nó chỉ chạy ở lần `bboxMay` đổi TIẾP THEO — mà trên một màn không ai chạm vào thì không có lần nào. Đúng cái ngoại lệ tác giả tự ghi (`ThanhCongCuCanh.tsx:169-172`: `refCanh.current` còn `null` ở lượt render đầu ⇒ "thử lại ở lượt `may` kế tiếp"). Hệ quả: người dùng mở Studio vẫn thấy đúng cảnh "45 máy co thành vệt mờ" của PH-29. Ảnh `V2-H5-studio-sau-auto-fit-1600.png` → `V2-H5-studio-sau-bam-mot-khoi-1600.png` |

---

## TỔNG

| nhóm | ĐẠT | CHẶN-ĐÚNG | SAI | HỎNG | N/A | tổng |
|---|---|---|---|---|---|---|
| nhóm 1 (R1–R8) | 8 | 0 | 0 | 0 | 0 | **8** |
| nhóm 2 (N-1…N-5) | 1 | 4 | 0 | 0 | 0 | **5** |
| nhóm 3 (H1–H5) | 3 | 0 | 2 | 0 | 0 | **5** |
| **TỔNG** | **12** | **4** | **2** | **0** | **0** | **18** |

**ô không có phán quyết: 0**

**Ba bản vá — kết luận theo từng bản:**
* **PH-12** (`noiCuaThucThe` + bỏ `factories[0]`/`toaNha[0]`): **ĐÓNG** — 5/5 ca (R1–R5) chuyển SAI→ĐẠT, và 3/3 đối chứng âm (N-1, N-2, và đối chứng dương trong N-2) chứng minh hàng rào không bị nới.
* **PH-13** (`ngoaiPhamVi` cho `LyDoManLine`): **ĐÓNG** — R6 đo được nhánh riêng, mã lý do riêng, câu riêng, và câu ấy không rò sự tồn tại.
* **PH-14/15/29**: **hai phần ba** — PH-14 (chọn toà/tầng) ĐÓNG (R7), PH-15 (ẩn nút theo đúng cổng) ĐÓNG (R8), **PH-29 (auto-fit) CHƯA ĐÓNG** (H5: không chạy ở lượt mount đầu). Ngoài ra bản vá PH-14 **đẻ một khuyết tật mới đã đo được**: H1 — đổi tầng làm **mất im lặng** thay đổi chưa lưu.
