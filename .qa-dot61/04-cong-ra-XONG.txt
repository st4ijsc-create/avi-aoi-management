ĐỢT 61 — CỔNG RA (mục 4 brief). Mọi số ĐO, không kế thừa.

 1. npm run check ...................... 0 lỗi (exit 0)   [chạy 5 lần, sau MỖI món]
 2. npm run check:tests ................ 27 lỗi  (trần ≤ 27) — Y HỆT nền 518434af
                                          0 trong twin3d · 0 trong e2e/
                                          8 tệp, đều thuộc server/services/aiLocal* +
                                          changeReason* + requireAnyPermissionKhoiC (nợ có trước)
 3. npm run i18n:check ................. exit 0
                                          0 placeholder mismatch · 0 NEW missing · 0 stale
                                          nền nợ 339→336 missing-in-all (THU HẸP 3, xem dưới)
 4. npm run lint:tokens ................ exit 0 · Δ VI PHẠM MỚI = 0
                                          nền 65 tệp/1.111 → sau 61 tệp/1.019
                                          0 tệp TĂNG vi phạm; 4 tệp biến mất vì đã bị xoá
                                          (đối chiếu từng tệp: .qa-dot61/lint-doi-chieu.txt)
 5. kiem-vo (scripts/kiem-vo-app-https.mjs) ... ĐẠT cả client/index.html và
                                          .qa-dot61/dist-61/public/index.html (tài nguyên ngoài 0)
 6. npx vitest run client/src/components/twin3d ... 108 tệp / 2.530 ca XANH (ngưỡng ≥108/2.530)
 7. npx vitest run phamVi (4 lưới phạm vi + census) ... 17 tệp / 437 ca XANH
      ⚠ brief ghi "180/180" — con số ấy KHÔNG khớp phép đo nào hôm nay; Đợt 58/59
        cũng ghi 17/437. Số 180 là di sản từ đợt cũ.
 8. npx vitest run client ............... 224 tệp / 4.238 ca; 3 tệp đỏ / 7 ca đỏ
      ĐỐI CHỨNG NỀN (git archive 518434af): **3 tệp đỏ / 7 ca đỏ, số y hệt** ⇒ nợ CÓ TRƯỚC.
      Chi tiết: .qa-dot61/06-test-do-XONG.txt
 9. BUILD vào outDir riêng .qa-dot61/dist-61 (NODE_ENV=production, đường tuyệt đối)
      914 tệp · bundle assets/index-B9QQr9Uo.js
      Dựng LẦN HAI (dist-61b): md5 914 dòng, **diff = 0 dòng khác** (G120)
      dist/ GỐC KHÔNG BỊ ĐỤNG: md5 dist/public/index.html trước=sau
        fe363553345bebdcfddad86c57b2dc9f
10. SERVER 3061 từ bản dựng ấy (3001/PID14228 + 3008/PID29676 của chủ sở hữu KHÔNG bị đụng)
      13 đường vào cũ .................. **13/13 ĐÚNG ĐÍCH** (đo bằng URL thật sau redirect)
      6 màn mở được .................... /twin · /twin/line/2 · /twin/may/14 ·
                                          /twin-studio · /command-center · /factory-command
      canvas (DOM + __soCanvas), so với NỀN dựng từ 518434af trên CÙNG cổng:
         /twin, /twin/line/2, /twin/may/14, /twin-studio : DOM 1 · kit 1  (nền = sau)
         /command-center : nền DOM **1** → sau DOM **0**   ★ ĐỔI ĐÚNG CHỖ
         /factory-command: nền DOM 0 → sau DOM 0           (không hỏng vì xoá FactoryScene3D)
      ★★★ `__soCanvas` = null ở CẢ hai bản tại /command-center (G99: `<Canvas>` cũ
          không đăng ký qua kit) ⇒ chỉ đọc `__soCanvas` như brief ghi là phép đo MÙ.
11. e2e bấm cảnh .................... **16/16 XANH** (--workers=1, spec
      e2e/twin-dot47-bam-canh.spec.ts, PLAYWRIGHT_BASE_URL=http://localhost:3061)
      ⚠ LƯỢT ĐẦU 0/16 ĐỎ — TOÀN BỘ ở `expect(login).toBe(200)` nhận **429**.
        Nguyên nhân: limiter đăng nhập dùng **Redis** (REDIS_URL đặt trong .env) ⇒
        thùng đếm DÙNG CHUNG với 3001/3008 của chủ sở hữu; 52 lượt đăng nhập của
        lượt chạy toàn project trước đó đã làm đầy cửa sổ 15 phút (max 30).
        Đây là hỏng THIẾT BỊ ĐO, không phải sản phẩm. Vá: khởi động lại server
        3061 với RATE_LIMIT_REDIS=false + AUTH_RATE_LIMIT_PER_15MIN=5000 — chỉ
        tác động tiến trình 3061 của tôi, KHÔNG sửa .env, KHÔNG đụng 3001/3008.
