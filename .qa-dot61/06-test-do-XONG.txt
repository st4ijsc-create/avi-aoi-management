ĐỢT 61 — TEST ĐỎ / BIẾN MẤT (mục 6 báo cáo)

A. TEST BIẾN MẤT — ĐÚNG 1 CA, CỐ Ý
   client/src/pages/twinHubTabBoTriXuong.unit.test.ts  2 ca → 1 ca
   Ca bỏ: "§2 TwinHub TABS — mục 'layout'" (đọc `TABS` của `TwinHub.tsx`, tệp đã xoá).
   §1 (`LayoutContent` render không có params.id) GIỮ — `LayoutContent` còn sống
   qua route `/layout/:id`. Tệp test XANH 1/1.

B. TEST ĐỎ — 3 TỆP / 7 CA, **CÓ TRƯỚC ĐỢT 61**, KHÔNG DO ĐỢT NÀY
   `npx vitest run client`  SAU = 224 tệp · 4.238 ca · **3 tệp đỏ / 7 ca đỏ**
   Ba tệp: clientErrorCoverage.unit.test.ts (2) · rawErrorMessageCensus.unit.test.ts (3)
           · viStringCoverage.unit.test.ts (2)

   ĐỐI CHỨNG — dựng CÂY NỀN tại HEAD `518434af` bằng `git archive` (đúng khuôn
   "census đo trên cây MỘT COMMIT, không trên worktree chung"), nối `node_modules`
   bằng junction, chạy đúng 3 tệp ấy:
        NỀN (518434af) : 3 tệp đỏ · **7 ca đỏ** / 11 xanh — SỐ Y HỆT
        SAU (đợt 61)   : 3 tệp đỏ · **7 ca đỏ**
   Từng con số cũng y hệt, không lệch một đơn vị:
        hình-3 (F12)            : nền 34 · sau 34
        phủ mã lỗi client       : nền 1  · sau 1   (client/src/pages/AOIPackages.tsx)
        chuỗi thô (F1)          : nền 4  · sau 4   (twin3d/occtWorker.ts, thiet-ke/NhapBanVe.tsx, …)
        cầu chì F1 "bơm nợ"     : nền 5≠1 · sau 5≠1 (hệ quả của nền 4, không phải lỗi riêng)
   Thêm bằng chứng độc lập: `git diff 518434af --stat` trên 3 tệp NGUỒN vi phạm
   + 3 tệp TEST = RỖNG ⇒ cả 6 tệp Y NGUYÊN như HEAD. Census cộng số ≥ 0 theo
   từng tệp, nên xoá tệp chỉ có thể LÀM GIẢM tổng, không thể đẻ ra vi phạm mới.

   ⇒ Kết luận: 7 ca đỏ là NỢ CÓ TRƯỚC (ngoài phạm vi QĐ-31). Đợt 61 làm lệch 0 ca.

C. CÁC SUITE KHÁC (sau đợt)
   vitest twin3d        : 108 tệp / 2.530 ca — XANH (đúng ngưỡng ≥ 108/2.530)
   vitest phamVi        : 17 tệp / 437 ca — XANH
   vitest client/src/pages: 12 tệp / 140 ca — XANH
