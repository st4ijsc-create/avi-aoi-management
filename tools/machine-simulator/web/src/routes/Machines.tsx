import { Factory } from "lucide-react"

import { PlaceholderScreen } from "@/components/PlaceholderScreen"

export default function Machines() {
  return (
    <PlaceholderScreen
      icon={Factory}
      title="Machines"
      description="Full roster with filters by device class and driver kind. Click a card on the Dashboard to jump straight to a machine's detail view."
      task="Coming in a later task"
    />
  )
}
