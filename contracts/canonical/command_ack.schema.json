{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "contracts/canonical/command_ack.schema.json",
  "title": "CommandAck",
  "description": "LDS-L1 canonical command acknowledgement (doc 44 G2.5). Published on syn/{site}/{area}/{line}/{cell}/{equip}/cmd/ack — one or more acks per command_id as it progresses (accepted → executing → done/failed). additionalProperties stays open.",
  "x-subject": "syn/+/+/+/+/+/cmd/ack",
  "x-owner": "platform",
  "type": "object",
  "properties": {
    "command_id": { "type": "string", "description": "Command id being acknowledged" },
    "status": { "type": "string", "enum": ["accepted", "rejected", "executing", "done", "failed"], "description": "Command lifecycle status" },
    "reason": { "type": "string", "description": "Reject/failure reason (required semantics when status is rejected/failed)" },
    "ts": { "type": "string", "description": "ISO-8601 timestamp of this ack" },
    "result": { "type": "object", "description": "Verb result payload when status=done" }
  },
  "required": ["command_id", "status", "ts"]
}
