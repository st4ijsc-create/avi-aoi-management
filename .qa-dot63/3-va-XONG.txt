Đợt 63 — mục 3: VÁ BẰNG MỘT HELPER DÙNG CHUNG + ABLATION HAI CÂY
=================================================================

A. TÌM TIỆN ÍCH SẴN CÓ TRƯỚC (đừng tạo trùng)
----------------------------------------------
  git ls-files | grep -iE "test-?utils?|testUtil|test.*helper"
    → chỉ có `server/contracts/machineDataContractV2.test-helpers.ts` (quy ước đặt CẠNH mã).
      KHÔNG có thư mục tiện ích test dùng chung nào.

  Quét EOL sẵn có trong 313 tệp ứng viên:
    → 9 tệp ĐÃ tự `.replace(/\r\n/g, "\n")` ngay sau `readFileSync` — toàn bộ thuộc họ
      `aiCodingWorkspace*` (boChonPhien, goiYTep, quanLyDuAnRepo, theDuyetDiffChonKhoi,
       aiCodingWorkspaceUx, aiCodingWorkspaceNhanKhoi, aiCodingWorkspacePhien,
       aiCodingWorkspaceUxLo1, aiCodingKhoiSua.stream).
    ⇒ Repo ĐÃ BIẾT lớp lỗi này và đã vá ĐÚNG — nhưng LẺ, ở một họ tệp. Đợt 63 gom về một chỗ.

  ⚠ LỖI ĐO CỦA CHÍNH TÔI: lần đầu tôi quét bằng `grep -rln 'replace(/\\r'` và được **0 tệp**
    ⇒ suýt viết "chưa ai từng chuẩn hoá EOL". Mẫu grep của tôi sai (bash nuốt escape).
    Chỉ khi đọc tay `aiCodingWorkspacePhien.unit.test.ts:40` mới lộ 9 tệp kia.

B. HELPER
----------
  shared/testing/docMaNguon.ts   (mới, 66 dòng — hơn nửa là docblock nêu vì sao)
    · `chuanHoaXuongDong(s)` — `\r\n?` → `\n`
    · `docMaNguon(p)`        — `readFileSync(p,"utf8")` + chuẩn hoá

  Đặt ở `shared/` vì CẢ client/, server/, scripts/ đều với tới qua alias `@shared/*`
  (đã có sẵn trong `vitest.config.ts` và `tsconfig.json#paths`) — `client/src/test-utils/`
  như brief gợi ý sẽ buộc test server import ngược vào client.

  Docblock ghi rõ RANH GIỚI: KHÔNG dùng helper cho test đọc tệp do CHÍNH MÃ ĐANG BỊ KIỂM
  ghi ra — ở đó `\r\n` là dữ kiện phải bắt.

C. PHẠM VI CHUYỂN ĐỔI
----------------------
  25 tệp · 59 chỗ đọc.  0 assertion nào về NỘI DUNG bị đổi (diff chỉ có: tên hàm đọc,
  một dòng import thêm, và các import `readFileSync` đã thành thừa thì bỏ).
  Commit 92ed9e21 — 26 tệp, +185 / −89.

  ⚠ Bắt được khi vá: worktree "LF" này thật ra LẪN EOL — 5 142 tệp `w/crlf`, 2 228 `w/lf`,
    17 `w/mixed`. 14/25 tệp tôi vá vốn là CRLF trên đĩa; dòng import tôi chèn ban đầu là LF
    ⇒ tự tay tạo 14 tệp `w/mixed`. Đã sửa: dòng chèn khớp EOL của từng tệp.
    Sau cùng: 14 `w/crlf` + 11 `w/lf`, 0 `w/mixed`.

D. ★★★ ABLATION HAI CÂY — CỔNG `vitest twin3d` (109 tệp / 2 539 ca)
--------------------------------------------------------------------
Cùng một lệnh `npx vitest run twin3d`, hai cây, hai commit:

  |            | cây LF  (D:/SOURCES/_twin_wt) | cây CRLF (clone sạch, w/crlf 100 %) |
  |------------|-------------------------------|--------------------------------------|
  | TRƯỚC vá   | 109 tệp / **2 539 XANH**      | 107/109 tệp · **2 537 xanh / 2 ĐỎ**  |
  | (5b67a1b4) |                               | badgeKepRiaDeNhan · manMayNoiVaoTrang|
  | SAU vá     | 109 tệp / **2 539 XANH**      | 109 tệp / **2 539 XANH**             |
  | (92ed9e21) |                               |                                      |

  Ô "trước vá / CRLF" là ô mang tin: nếu nó XANH thì bản vá không chứng minh được gì.

E. ABLATION MỞ RỘNG — bộ 313 tệp ứng viên
------------------------------------------
  | | CRLF | LF |
  |---|---|---|
  | trước vá | 5 435 ca · 68 đỏ | 5 466 ca · 70 đỏ |
  | sau vá   | 5 436 ca · 66 đỏ | 5 473 ca · 69 đỏ |

  Đỏ CHỈ-trên-CRLF sau vá: 3 ca / 2 tệp — `pointLimitSpecCensus`, `vramReadModel.roster`.
  Chạy RIÊNG trên chính cây CRLF: **cả hai XANH** (12/12 và 8/8) ⇒ là đua tệp probe tạm
  (mục 2.D), KHÔNG phải EOL.
  ⇒ **0 ca còn phụ thuộc EOL đo được** trên bộ 313.
  (Giới hạn: phép đo này mù với biến thể CÂM — xem mục 2.E.)

F. HỒI QUY — bản vá có làm đỏ gì mới không?
--------------------------------------------
  LF trước 70 đỏ → LF sau 69 đỏ. Hiệu hai chiều: 3 "đỏ mới", 4 "hết đỏ" — tất cả đều là
  nhiễu đua probe / thứ tự (xacThucBeMatRest, vramAllocationSites, fakeUtcCensus,
  capComponentGhiThat, aiSpecialistAgentRouter).
  Kiểm riêng từng ca:
    · fakeUtcCensus       chạy riêng → 27/27 XANH.
    · vramAllocationSites chạy riêng → 2 đỏ; **và cũng 2 đỏ trên bản HEAD chưa vá**
      (đã đo bằng cách trả tệp về HEAD rồi chạy lại) ⇒ nợ có sẵn, không do tôi.
    · xacThucBeMatRest    — tệp này TÔI KHÔNG ĐỤNG.
  ⇒ Bản vá không thêm một ca đỏ nào.
